/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.phenoml.api.resources.construe.codes;

import com.phenoml.api.core.ClientOptions;
import com.phenoml.api.core.RequestOptions;
import com.phenoml.api.resources.construe.codes.requests.ExtractRequest;
import com.phenoml.api.resources.construe.codes.requests.ListRequest;
import com.phenoml.api.resources.construe.codes.requests.LookupRequest;
import com.phenoml.api.resources.construe.codes.requests.SearchSemanticRequest;
import com.phenoml.api.resources.construe.codes.requests.SearchTextRequest;
import com.phenoml.api.resources.construe.types.ExtractCodesResult;
import com.phenoml.api.resources.construe.types.GetCodeResponse;
import com.phenoml.api.resources.construe.types.ListCodesResponse;
import com.phenoml.api.resources.construe.types.SemanticSearchResponse;
import com.phenoml.api.resources.construe.types.TextSearchResponse;

public class CodesClient {
    protected final ClientOptions clientOptions;

    private final RawCodesClient rawClient;

    public CodesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawCodesClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawCodesClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Converts natural language text into structured medical codes.
     * <p>Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.</p>
     */
    public ExtractCodesResult extract(ExtractRequest request) {
        return this.rawClient.extract(request).body();
    }

    /**
     * Converts natural language text into structured medical codes.
     * <p>Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.</p>
     */
    public ExtractCodesResult extract(ExtractRequest request, RequestOptions requestOptions) {
        return this.rawClient.extract(request, requestOptions).body();
    }

    /**
     * Returns a paginated list of all codes in the specified code system from the terminology server.
     * <p>Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.</p>
     */
    public ListCodesResponse list(String codesystem) {
        return this.rawClient.list(codesystem).body();
    }

    /**
     * Returns a paginated list of all codes in the specified code system from the terminology server.
     * <p>Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.</p>
     */
    public ListCodesResponse list(String codesystem, RequestOptions requestOptions) {
        return this.rawClient.list(codesystem, requestOptions).body();
    }

    /**
     * Returns a paginated list of all codes in the specified code system from the terminology server.
     * <p>Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.</p>
     */
    public ListCodesResponse list(String codesystem, ListRequest request) {
        return this.rawClient.list(codesystem, request).body();
    }

    /**
     * Returns a paginated list of all codes in the specified code system from the terminology server.
     * <p>Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.</p>
     */
    public ListCodesResponse list(String codesystem, ListRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(codesystem, request, requestOptions).body();
    }

    /**
     * Looks up a specific code in the terminology server and returns its details.
     * <p>Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.</p>
     */
    public GetCodeResponse lookup(String codesystem, String codeId) {
        return this.rawClient.lookup(codesystem, codeId).body();
    }

    /**
     * Looks up a specific code in the terminology server and returns its details.
     * <p>Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.</p>
     */
    public GetCodeResponse lookup(String codesystem, String codeId, RequestOptions requestOptions) {
        return this.rawClient.lookup(codesystem, codeId, requestOptions).body();
    }

    /**
     * Looks up a specific code in the terminology server and returns its details.
     * <p>Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.</p>
     */
    public GetCodeResponse lookup(String codesystem, String codeId, LookupRequest request) {
        return this.rawClient.lookup(codesystem, codeId, request).body();
    }

    /**
     * Looks up a specific code in the terminology server and returns its details.
     * <p>Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.</p>
     */
    public GetCodeResponse lookup(
            String codesystem, String codeId, LookupRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .lookup(codesystem, codeId, request, requestOptions)
                .body();
    }

    /**
     * Performs semantic similarity search using vector embeddings.
     * <p><strong>Availability</strong>: This endpoint works for both <strong>built-in and custom</strong> code systems.</p>
     * <p><strong>When to use</strong>: Best for natural language queries where you want to find conceptually
     * related codes, even when different terminology is used. The search understands meaning,
     * not just keywords.</p>
     * <p><strong>Examples</strong>:</p>
     * <ul>
     * <li>Query &quot;trouble breathing at night&quot; finds codes like &quot;Sleep apnea&quot;, &quot;Orthopnea&quot;,
     * &quot;Nocturnal dyspnea&quot; — semantically related but no exact keyword matches</li>
     * <li>Query &quot;heart problems&quot; finds &quot;Myocardial infarction&quot;, &quot;Cardiac arrest&quot;, &quot;Arrhythmia&quot;</li>
     * </ul>
     * <p><strong>Trade-offs</strong>: Slower than text search (requires embedding generation), but finds
     * conceptually similar results that keyword search would miss.</p>
     * <p>See also: <code>/search/text</code> for faster keyword-based lookup with typo tolerance.</p>
     * <p>Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.</p>
     */
    public SemanticSearchResponse searchSemantic(String codesystem, SearchSemanticRequest request) {
        return this.rawClient.searchSemantic(codesystem, request).body();
    }

    /**
     * Performs semantic similarity search using vector embeddings.
     * <p><strong>Availability</strong>: This endpoint works for both <strong>built-in and custom</strong> code systems.</p>
     * <p><strong>When to use</strong>: Best for natural language queries where you want to find conceptually
     * related codes, even when different terminology is used. The search understands meaning,
     * not just keywords.</p>
     * <p><strong>Examples</strong>:</p>
     * <ul>
     * <li>Query &quot;trouble breathing at night&quot; finds codes like &quot;Sleep apnea&quot;, &quot;Orthopnea&quot;,
     * &quot;Nocturnal dyspnea&quot; — semantically related but no exact keyword matches</li>
     * <li>Query &quot;heart problems&quot; finds &quot;Myocardial infarction&quot;, &quot;Cardiac arrest&quot;, &quot;Arrhythmia&quot;</li>
     * </ul>
     * <p><strong>Trade-offs</strong>: Slower than text search (requires embedding generation), but finds
     * conceptually similar results that keyword search would miss.</p>
     * <p>See also: <code>/search/text</code> for faster keyword-based lookup with typo tolerance.</p>
     * <p>Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.</p>
     */
    public SemanticSearchResponse searchSemantic(
            String codesystem, SearchSemanticRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .searchSemantic(codesystem, request, requestOptions)
                .body();
    }

    /**
     * Performs fast full-text search over code IDs and descriptions.
     * <p><strong>Availability</strong>: This endpoint is only available for <strong>built-in code systems</strong>.
     * Custom code systems uploaded via <code>/construe/upload</code> are not indexed for full-text search
     * and will return empty results. Use <code>/search/semantic</code> to search custom code systems.</p>
     * <p><strong>When to use</strong>: Best for autocomplete UIs, code lookup, or when users know part of
     * the code ID or specific keywords. Fast response times suitable for typeahead interfaces.</p>
     * <p><strong>Features</strong>:</p>
     * <ul>
     * <li>Substring matching on code IDs (e.g., &quot;11.65&quot; finds &quot;E11.65&quot;)</li>
     * <li>Typo tolerance on descriptions (not on code IDs)</li>
     * <li>Fast response times (~10-50ms)</li>
     * </ul>
     * <p><strong>Examples</strong>:</p>
     * <ul>
     * <li>Query &quot;E11&quot; finds all codes starting with E11 (diabetes codes)</li>
     * <li>Query &quot;diabtes&quot; (typo) still finds &quot;diabetes&quot; codes</li>
     * </ul>
     * <p><strong>Trade-offs</strong>: Faster than semantic search, but only matches keywords/substrings.
     * Won't find conceptually related codes with different terminology.</p>
     * <p>See also: <code>/search/semantic</code> for finding conceptually similar codes.</p>
     * <p>Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.</p>
     */
    public TextSearchResponse searchText(String codesystem, SearchTextRequest request) {
        return this.rawClient.searchText(codesystem, request).body();
    }

    /**
     * Performs fast full-text search over code IDs and descriptions.
     * <p><strong>Availability</strong>: This endpoint is only available for <strong>built-in code systems</strong>.
     * Custom code systems uploaded via <code>/construe/upload</code> are not indexed for full-text search
     * and will return empty results. Use <code>/search/semantic</code> to search custom code systems.</p>
     * <p><strong>When to use</strong>: Best for autocomplete UIs, code lookup, or when users know part of
     * the code ID or specific keywords. Fast response times suitable for typeahead interfaces.</p>
     * <p><strong>Features</strong>:</p>
     * <ul>
     * <li>Substring matching on code IDs (e.g., &quot;11.65&quot; finds &quot;E11.65&quot;)</li>
     * <li>Typo tolerance on descriptions (not on code IDs)</li>
     * <li>Fast response times (~10-50ms)</li>
     * </ul>
     * <p><strong>Examples</strong>:</p>
     * <ul>
     * <li>Query &quot;E11&quot; finds all codes starting with E11 (diabetes codes)</li>
     * <li>Query &quot;diabtes&quot; (typo) still finds &quot;diabetes&quot; codes</li>
     * </ul>
     * <p><strong>Trade-offs</strong>: Faster than semantic search, but only matches keywords/substrings.
     * Won't find conceptually related codes with different terminology.</p>
     * <p>See also: <code>/search/semantic</code> for finding conceptually similar codes.</p>
     * <p>Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.</p>
     */
    public TextSearchResponse searchText(String codesystem, SearchTextRequest request, RequestOptions requestOptions) {
        return this.rawClient.searchText(codesystem, request, requestOptions).body();
    }
}
