/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.phenoml.api.resources.lang2fhirbatch.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.phenoml.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = BatchJob.Builder.class)
public final class BatchJob implements IBatchJob {
    private final String jobId;

    private final Optional<String> requestId;

    private final BatchJobStatus status;

    private final boolean finalized;

    private final int totalItems;

    private final Optional<BatchError> error;

    private final OffsetDateTime createdAt;

    private final OffsetDateTime updatedAt;

    private final Optional<OffsetDateTime> completedAt;

    private final OffsetDateTime expiresAt;

    private final Map<String, Object> additionalProperties;

    private BatchJob(
            String jobId,
            Optional<String> requestId,
            BatchJobStatus status,
            boolean finalized,
            int totalItems,
            Optional<BatchError> error,
            OffsetDateTime createdAt,
            OffsetDateTime updatedAt,
            Optional<OffsetDateTime> completedAt,
            OffsetDateTime expiresAt,
            Map<String, Object> additionalProperties) {
        this.jobId = jobId;
        this.requestId = requestId;
        this.status = status;
        this.finalized = finalized;
        this.totalItems = totalItems;
        this.error = error;
        this.createdAt = createdAt;
        this.updatedAt = updatedAt;
        this.completedAt = completedAt;
        this.expiresAt = expiresAt;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Server-assigned job identifier.
     */
    @JsonProperty("job_id")
    @java.lang.Override
    public String getJobId() {
        return jobId;
    }

    /**
     * @return The idempotency token supplied at create, if any.
     */
    @JsonProperty("request_id")
    @java.lang.Override
    public Optional<String> getRequestId() {
        return requestId;
    }

    /**
     * @return Job status. <code>completed</code> means every item has finished — some may have
     * failed, so check <code>counts</code> for the split. <code>failed</code> is a whole-job
     * failure (the job could not run at all), distinct from individual item
     * failures, which never fail the job.
     */
    @JsonProperty("status")
    public BatchJobStatus getStatus() {
        return status;
    }

    /**
     * @return Whether the job's item set has been sealed.
     */
    @JsonProperty("finalized")
    @java.lang.Override
    public boolean getFinalized() {
        return finalized;
    }

    /**
     * @return The sealed item count. It is 0 until the job is finalized, so an
     * upload response always reports 0; poll the job after finalize for the
     * real count.
     */
    @JsonProperty("total_items")
    @java.lang.Override
    public int getTotalItems() {
        return totalItems;
    }

    /**
     * @return A whole-job failure. Present only on a <code>failed</code> job.
     */
    @JsonProperty("error")
    @java.lang.Override
    public Optional<BatchError> getError() {
        return error;
    }

    @JsonProperty("created_at")
    @java.lang.Override
    public OffsetDateTime getCreatedAt() {
        return createdAt;
    }

    @JsonProperty("updated_at")
    @java.lang.Override
    public OffsetDateTime getUpdatedAt() {
        return updatedAt;
    }

    /**
     * @return When the job finished. Absent until then.
     */
    @JsonProperty("completed_at")
    @java.lang.Override
    public Optional<OffsetDateTime> getCompletedAt() {
        return completedAt;
    }

    /**
     * @return When the job and its stored inputs and results are deleted. Set 7
     * days out, with the clock restarting when the job reaches <code>completed</code>
     * or <code>failed</code>. At expiry the job's <code>request_id</code> is freed for reuse.
     */
    @JsonProperty("expires_at")
    @java.lang.Override
    public OffsetDateTime getExpiresAt() {
        return expiresAt;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof BatchJob && equalTo((BatchJob) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(BatchJob other) {
        return jobId.equals(other.jobId)
                && requestId.equals(other.requestId)
                && status.equals(other.status)
                && finalized == other.finalized
                && totalItems == other.totalItems
                && error.equals(other.error)
                && createdAt.equals(other.createdAt)
                && updatedAt.equals(other.updatedAt)
                && completedAt.equals(other.completedAt)
                && expiresAt.equals(other.expiresAt);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.jobId,
                this.requestId,
                this.status,
                this.finalized,
                this.totalItems,
                this.error,
                this.createdAt,
                this.updatedAt,
                this.completedAt,
                this.expiresAt);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static JobIdStage builder() {
        return new Builder();
    }

    public interface JobIdStage {
        /**
         * <p>Server-assigned job identifier.</p>
         */
        StatusStage jobId(@NotNull String jobId);

        Builder from(BatchJob other);
    }

    public interface StatusStage {
        /**
         * <p>Job status. <code>completed</code> means every item has finished — some may have
         * failed, so check <code>counts</code> for the split. <code>failed</code> is a whole-job
         * failure (the job could not run at all), distinct from individual item
         * failures, which never fail the job.</p>
         */
        FinalizedStage status(@NotNull BatchJobStatus status);
    }

    public interface FinalizedStage {
        /**
         * <p>Whether the job's item set has been sealed.</p>
         */
        TotalItemsStage finalized(boolean finalized);
    }

    public interface TotalItemsStage {
        /**
         * <p>The sealed item count. It is 0 until the job is finalized, so an
         * upload response always reports 0; poll the job after finalize for the
         * real count.</p>
         */
        CreatedAtStage totalItems(int totalItems);
    }

    public interface CreatedAtStage {
        UpdatedAtStage createdAt(@NotNull OffsetDateTime createdAt);
    }

    public interface UpdatedAtStage {
        ExpiresAtStage updatedAt(@NotNull OffsetDateTime updatedAt);
    }

    public interface ExpiresAtStage {
        /**
         * <p>When the job and its stored inputs and results are deleted. Set 7
         * days out, with the clock restarting when the job reaches <code>completed</code>
         * or <code>failed</code>. At expiry the job's <code>request_id</code> is freed for reuse.</p>
         */
        _FinalStage expiresAt(@NotNull OffsetDateTime expiresAt);
    }

    public interface _FinalStage {
        BatchJob build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The idempotency token supplied at create, if any.</p>
         */
        _FinalStage requestId(Optional<String> requestId);

        _FinalStage requestId(String requestId);

        /**
         * <p>A whole-job failure. Present only on a <code>failed</code> job.</p>
         */
        _FinalStage error(Optional<BatchError> error);

        _FinalStage error(BatchError error);

        /**
         * <p>When the job finished. Absent until then.</p>
         */
        _FinalStage completedAt(Optional<OffsetDateTime> completedAt);

        _FinalStage completedAt(OffsetDateTime completedAt);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements JobIdStage,
                    StatusStage,
                    FinalizedStage,
                    TotalItemsStage,
                    CreatedAtStage,
                    UpdatedAtStage,
                    ExpiresAtStage,
                    _FinalStage {
        private String jobId;

        private BatchJobStatus status;

        private boolean finalized;

        private int totalItems;

        private OffsetDateTime createdAt;

        private OffsetDateTime updatedAt;

        private OffsetDateTime expiresAt;

        private Optional<OffsetDateTime> completedAt = Optional.empty();

        private Optional<BatchError> error = Optional.empty();

        private Optional<String> requestId = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(BatchJob other) {
            jobId(other.getJobId());
            requestId(other.getRequestId());
            status(other.getStatus());
            finalized(other.getFinalized());
            totalItems(other.getTotalItems());
            error(other.getError());
            createdAt(other.getCreatedAt());
            updatedAt(other.getUpdatedAt());
            completedAt(other.getCompletedAt());
            expiresAt(other.getExpiresAt());
            return this;
        }

        /**
         * <p>Server-assigned job identifier.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("job_id")
        public StatusStage jobId(@NotNull String jobId) {
            this.jobId = Objects.requireNonNull(jobId, "jobId must not be null");
            return this;
        }

        /**
         * <p>Job status. <code>completed</code> means every item has finished — some may have
         * failed, so check <code>counts</code> for the split. <code>failed</code> is a whole-job
         * failure (the job could not run at all), distinct from individual item
         * failures, which never fail the job.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public FinalizedStage status(@NotNull BatchJobStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>Whether the job's item set has been sealed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("finalized")
        public TotalItemsStage finalized(boolean finalized) {
            this.finalized = finalized;
            return this;
        }

        /**
         * <p>The sealed item count. It is 0 until the job is finalized, so an
         * upload response always reports 0; poll the job after finalize for the
         * real count.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("total_items")
        public CreatedAtStage totalItems(int totalItems) {
            this.totalItems = totalItems;
            return this;
        }

        @java.lang.Override
        @JsonSetter("created_at")
        public UpdatedAtStage createdAt(@NotNull OffsetDateTime createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("updated_at")
        public ExpiresAtStage updatedAt(@NotNull OffsetDateTime updatedAt) {
            this.updatedAt = Objects.requireNonNull(updatedAt, "updatedAt must not be null");
            return this;
        }

        /**
         * <p>When the job and its stored inputs and results are deleted. Set 7
         * days out, with the clock restarting when the job reaches <code>completed</code>
         * or <code>failed</code>. At expiry the job's <code>request_id</code> is freed for reuse.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("expires_at")
        public _FinalStage expiresAt(@NotNull OffsetDateTime expiresAt) {
            this.expiresAt = Objects.requireNonNull(expiresAt, "expiresAt must not be null");
            return this;
        }

        /**
         * <p>When the job finished. Absent until then.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage completedAt(OffsetDateTime completedAt) {
            this.completedAt = Optional.ofNullable(completedAt);
            return this;
        }

        /**
         * <p>When the job finished. Absent until then.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "completed_at", nulls = Nulls.SKIP)
        public _FinalStage completedAt(Optional<OffsetDateTime> completedAt) {
            this.completedAt = completedAt;
            return this;
        }

        /**
         * <p>A whole-job failure. Present only on a <code>failed</code> job.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage error(BatchError error) {
            this.error = Optional.ofNullable(error);
            return this;
        }

        /**
         * <p>A whole-job failure. Present only on a <code>failed</code> job.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "error", nulls = Nulls.SKIP)
        public _FinalStage error(Optional<BatchError> error) {
            this.error = error;
            return this;
        }

        /**
         * <p>The idempotency token supplied at create, if any.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage requestId(String requestId) {
            this.requestId = Optional.ofNullable(requestId);
            return this;
        }

        /**
         * <p>The idempotency token supplied at create, if any.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "request_id", nulls = Nulls.SKIP)
        public _FinalStage requestId(Optional<String> requestId) {
            this.requestId = requestId;
            return this;
        }

        @java.lang.Override
        public BatchJob build() {
            return new BatchJob(
                    jobId,
                    requestId,
                    status,
                    finalized,
                    totalItems,
                    error,
                    createdAt,
                    updatedAt,
                    completedAt,
                    expiresAt,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
