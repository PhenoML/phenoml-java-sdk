/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.phenoml.api.resources.lang2fhirbatch.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.phenoml.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = BatchItemStatus.Builder.class)
public final class BatchItemStatus {
    private final String itemId;

    private final Optional<String> id;

    private final BatchItemStatusStatus status;

    private final int attempts;

    private final Optional<Long> detectRetries;

    private final Optional<Long> resultSize;

    private final Optional<BatchError> error;

    private final Optional<OffsetDateTime> completedAt;

    private final Map<String, Object> additionalProperties;

    private BatchItemStatus(
            String itemId,
            Optional<String> id,
            BatchItemStatusStatus status,
            int attempts,
            Optional<Long> detectRetries,
            Optional<Long> resultSize,
            Optional<BatchError> error,
            Optional<OffsetDateTime> completedAt,
            Map<String, Object> additionalProperties) {
        this.itemId = itemId;
        this.id = id;
        this.status = status;
        this.attempts = attempts;
        this.detectRetries = detectRetries;
        this.resultSize = resultSize;
        this.error = error;
        this.completedAt = completedAt;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Server-assigned opaque item ID, used in result URLs.
     */
    @JsonProperty("item_id")
    public String getItemId() {
        return itemId;
    }

    /**
     * @return The caller's correlation label, echoed back; omitted when none was supplied.
     */
    @JsonProperty("id")
    public Optional<String> getId() {
        return id;
    }

    /**
     * @return Item status.
     */
    @JsonProperty("status")
    public BatchItemStatusStatus getStatus() {
        return status;
    }

    /**
     * @return Number of processing attempts so far. An item runs up to 3 attempts,
     * and only an interruption — a worker preemption or a recovered internal
     * error — is retried; a conversion error fails the item with no retry.
     */
    @JsonProperty("attempts")
    public int getAttempts() {
        return attempts;
    }

    /**
     * @return How many times concept detection was re-run for the item because a
     * pass returned no concepts. Present only on a succeeded item whose
     * detection re-ran at least once; omitted otherwise.
     */
    @JsonProperty("detect_retries")
    public Optional<Long> getDetectRetries() {
        return detectRetries;
    }

    /**
     * @return Size in bytes of the item's stored result. Present once the item has succeeded.
     */
    @JsonProperty("result_size")
    public Optional<Long> getResultSize() {
        return resultSize;
    }

    @JsonProperty("error")
    public Optional<BatchError> getError() {
        return error;
    }

    /**
     * @return When the item finished. Absent until then.
     */
    @JsonProperty("completed_at")
    public Optional<OffsetDateTime> getCompletedAt() {
        return completedAt;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof BatchItemStatus && equalTo((BatchItemStatus) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(BatchItemStatus other) {
        return itemId.equals(other.itemId)
                && id.equals(other.id)
                && status.equals(other.status)
                && attempts == other.attempts
                && detectRetries.equals(other.detectRetries)
                && resultSize.equals(other.resultSize)
                && error.equals(other.error)
                && completedAt.equals(other.completedAt);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.itemId,
                this.id,
                this.status,
                this.attempts,
                this.detectRetries,
                this.resultSize,
                this.error,
                this.completedAt);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static ItemIdStage builder() {
        return new Builder();
    }

    public interface ItemIdStage {
        /**
         * <p>Server-assigned opaque item ID, used in result URLs.</p>
         */
        StatusStage itemId(@NotNull String itemId);

        Builder from(BatchItemStatus other);
    }

    public interface StatusStage {
        /**
         * <p>Item status.</p>
         */
        AttemptsStage status(@NotNull BatchItemStatusStatus status);
    }

    public interface AttemptsStage {
        /**
         * <p>Number of processing attempts so far. An item runs up to 3 attempts,
         * and only an interruption — a worker preemption or a recovered internal
         * error — is retried; a conversion error fails the item with no retry.</p>
         */
        _FinalStage attempts(int attempts);
    }

    public interface _FinalStage {
        BatchItemStatus build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The caller's correlation label, echoed back; omitted when none was supplied.</p>
         */
        _FinalStage id(Optional<String> id);

        _FinalStage id(String id);

        /**
         * <p>How many times concept detection was re-run for the item because a
         * pass returned no concepts. Present only on a succeeded item whose
         * detection re-ran at least once; omitted otherwise.</p>
         */
        _FinalStage detectRetries(Optional<Long> detectRetries);

        _FinalStage detectRetries(Long detectRetries);

        /**
         * <p>Size in bytes of the item's stored result. Present once the item has succeeded.</p>
         */
        _FinalStage resultSize(Optional<Long> resultSize);

        _FinalStage resultSize(Long resultSize);

        _FinalStage error(Optional<BatchError> error);

        _FinalStage error(BatchError error);

        /**
         * <p>When the item finished. Absent until then.</p>
         */
        _FinalStage completedAt(Optional<OffsetDateTime> completedAt);

        _FinalStage completedAt(OffsetDateTime completedAt);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements ItemIdStage, StatusStage, AttemptsStage, _FinalStage {
        private String itemId;

        private BatchItemStatusStatus status;

        private int attempts;

        private Optional<OffsetDateTime> completedAt = Optional.empty();

        private Optional<BatchError> error = Optional.empty();

        private Optional<Long> resultSize = Optional.empty();

        private Optional<Long> detectRetries = Optional.empty();

        private Optional<String> id = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(BatchItemStatus other) {
            itemId(other.getItemId());
            id(other.getId());
            status(other.getStatus());
            attempts(other.getAttempts());
            detectRetries(other.getDetectRetries());
            resultSize(other.getResultSize());
            error(other.getError());
            completedAt(other.getCompletedAt());
            return this;
        }

        /**
         * <p>Server-assigned opaque item ID, used in result URLs.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("item_id")
        public StatusStage itemId(@NotNull String itemId) {
            this.itemId = Objects.requireNonNull(itemId, "itemId must not be null");
            return this;
        }

        /**
         * <p>Item status.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public AttemptsStage status(@NotNull BatchItemStatusStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>Number of processing attempts so far. An item runs up to 3 attempts,
         * and only an interruption — a worker preemption or a recovered internal
         * error — is retried; a conversion error fails the item with no retry.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("attempts")
        public _FinalStage attempts(int attempts) {
            this.attempts = attempts;
            return this;
        }

        /**
         * <p>When the item finished. Absent until then.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage completedAt(OffsetDateTime completedAt) {
            this.completedAt = Optional.ofNullable(completedAt);
            return this;
        }

        /**
         * <p>When the item finished. Absent until then.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "completed_at", nulls = Nulls.SKIP)
        public _FinalStage completedAt(Optional<OffsetDateTime> completedAt) {
            this.completedAt = completedAt;
            return this;
        }

        @java.lang.Override
        public _FinalStage error(BatchError error) {
            this.error = Optional.ofNullable(error);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "error", nulls = Nulls.SKIP)
        public _FinalStage error(Optional<BatchError> error) {
            this.error = error;
            return this;
        }

        /**
         * <p>Size in bytes of the item's stored result. Present once the item has succeeded.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resultSize(Long resultSize) {
            this.resultSize = Optional.ofNullable(resultSize);
            return this;
        }

        /**
         * <p>Size in bytes of the item's stored result. Present once the item has succeeded.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "result_size", nulls = Nulls.SKIP)
        public _FinalStage resultSize(Optional<Long> resultSize) {
            this.resultSize = resultSize;
            return this;
        }

        /**
         * <p>How many times concept detection was re-run for the item because a
         * pass returned no concepts. Present only on a succeeded item whose
         * detection re-ran at least once; omitted otherwise.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage detectRetries(Long detectRetries) {
            this.detectRetries = Optional.ofNullable(detectRetries);
            return this;
        }

        /**
         * <p>How many times concept detection was re-run for the item because a
         * pass returned no concepts. Present only on a succeeded item whose
         * detection re-ran at least once; omitted otherwise.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "detect_retries", nulls = Nulls.SKIP)
        public _FinalStage detectRetries(Optional<Long> detectRetries) {
            this.detectRetries = detectRetries;
            return this;
        }

        /**
         * <p>The caller's correlation label, echoed back; omitted when none was supplied.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage id(String id) {
            this.id = Optional.ofNullable(id);
            return this;
        }

        /**
         * <p>The caller's correlation label, echoed back; omitted when none was supplied.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "id", nulls = Nulls.SKIP)
        public _FinalStage id(Optional<String> id) {
            this.id = id;
            return this;
        }

        @java.lang.Override
        public BatchItemStatus build() {
            return new BatchItemStatus(
                    itemId, id, status, attempts, detectRetries, resultSize, error, completedAt, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
