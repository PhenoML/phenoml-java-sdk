/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.phenoml.api.resources.workflows;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.phenoml.api.core.ClientOptions;
import com.phenoml.api.core.MediaTypes;
import com.phenoml.api.core.ObjectMappers;
import com.phenoml.api.core.PhenomlApiApiException;
import com.phenoml.api.core.PhenomlApiException;
import com.phenoml.api.core.PhenomlApiHttpResponse;
import com.phenoml.api.core.QueryStringMapper;
import com.phenoml.api.core.RequestOptions;
import com.phenoml.api.resources.workflows.errors.BadRequestError;
import com.phenoml.api.resources.workflows.errors.ForbiddenError;
import com.phenoml.api.resources.workflows.errors.InternalServerError;
import com.phenoml.api.resources.workflows.errors.NotFoundError;
import com.phenoml.api.resources.workflows.errors.UnauthorizedError;
import com.phenoml.api.resources.workflows.requests.CreateWorkflowRequest;
import com.phenoml.api.resources.workflows.requests.ExecuteWorkflowRequest;
import com.phenoml.api.resources.workflows.requests.UpdateWorkflowRequest;
import com.phenoml.api.resources.workflows.requests.WorkflowsGetRequest;
import com.phenoml.api.resources.workflows.requests.WorkflowsListRequest;
import com.phenoml.api.resources.workflows.types.CreateWorkflowResponse;
import com.phenoml.api.resources.workflows.types.ExecuteWorkflowResponse;
import com.phenoml.api.resources.workflows.types.ListWorkflowsResponse;
import com.phenoml.api.resources.workflows.types.WorkflowsDeleteResponse;
import com.phenoml.api.resources.workflows.types.WorkflowsGetResponse;
import com.phenoml.api.resources.workflows.types.WorkflowsUpdateResponse;
import java.io.IOException;
import java.lang.Exception;
import java.lang.Object;
import java.lang.RuntimeException;
import java.lang.String;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawWorkflowsClient {
  protected final ClientOptions clientOptions;

  public RawWorkflowsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Retrieves all workflow definitions for the authenticated user
   */
  public PhenomlApiHttpResponse<ListWorkflowsResponse> list() {
    return list(WorkflowsListRequest.builder().build());
  }

  /**
   * Retrieves all workflow definitions for the authenticated user
   */
  public PhenomlApiHttpResponse<ListWorkflowsResponse> list(RequestOptions requestOptions) {
    return list(WorkflowsListRequest.builder().build(),requestOptions);
  }

  /**
   * Retrieves all workflow definitions for the authenticated user
   */
  public PhenomlApiHttpResponse<ListWorkflowsResponse> list(WorkflowsListRequest request) {
    return list(request,null);
  }

  /**
   * Retrieves all workflow definitions for the authenticated user
   */
  public PhenomlApiHttpResponse<ListWorkflowsResponse> list(WorkflowsListRequest request,
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("workflows");if (request.getVerbose().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "verbose", request.getVerbose().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new PhenomlApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListWorkflowsResponse.class), response);
        }
        try {
          switch (response.code()) {
            case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new PhenomlApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new PhenomlApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Creates a new workflow definition with graph generation from workflow instructions
     */
    public PhenomlApiHttpResponse<CreateWorkflowResponse> create(CreateWorkflowRequest request) {
      return create(request,null);
    }

    /**
     * Creates a new workflow definition with graph generation from workflow instructions
     */
    public PhenomlApiHttpResponse<CreateWorkflowResponse> create(CreateWorkflowRequest request,
        RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("workflows");if (request.getVerbose().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "verbose", request.getVerbose().get(), false);
        }
        if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(Exception e) {
          throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new PhenomlApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateWorkflowResponse.class), response);
          }
          try {
            switch (response.code()) {
              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new PhenomlApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new PhenomlApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Retrieves a workflow definition by its ID
       */
      public PhenomlApiHttpResponse<WorkflowsGetResponse> get(String id) {
        return get(id,WorkflowsGetRequest.builder().build());
      }

      /**
       * Retrieves a workflow definition by its ID
       */
      public PhenomlApiHttpResponse<WorkflowsGetResponse> get(String id,
          RequestOptions requestOptions) {
        return get(id,WorkflowsGetRequest.builder().build(),requestOptions);
      }

      /**
       * Retrieves a workflow definition by its ID
       */
      public PhenomlApiHttpResponse<WorkflowsGetResponse> get(String id,
          WorkflowsGetRequest request) {
        return get(id,request,null);
      }

      /**
       * Retrieves a workflow definition by its ID
       */
      public PhenomlApiHttpResponse<WorkflowsGetResponse> get(String id,
          WorkflowsGetRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("workflows")
          .addPathSegment(id);if (request.getVerbose().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "verbose", request.getVerbose().get(), false);
          }
          if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new PhenomlApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, WorkflowsGetResponse.class), response);
            }
            try {
              switch (response.code()) {
                case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PhenomlApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new PhenomlApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Updates an existing workflow definition
         */
        public PhenomlApiHttpResponse<WorkflowsUpdateResponse> update(String id,
            UpdateWorkflowRequest request) {
          return update(id,request,null);
        }

        /**
         * Updates an existing workflow definition
         */
        public PhenomlApiHttpResponse<WorkflowsUpdateResponse> update(String id,
            UpdateWorkflowRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("workflows")
            .addPathSegment(id);if (request.getVerbose().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "verbose", request.getVerbose().get(), false);
            }
            if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
            }
            catch(Exception e) {
              throw new RuntimeException(e);
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("PUT", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new PhenomlApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, WorkflowsUpdateResponse.class), response);
              }
              try {
                switch (response.code()) {
                  case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new PhenomlApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new PhenomlApiException("Network error executing HTTP request", e);
            }
          }

          /**
           * Deletes a workflow definition by its ID
           */
          public PhenomlApiHttpResponse<WorkflowsDeleteResponse> delete(String id) {
            return delete(id,null);
          }

          /**
           * Deletes a workflow definition by its ID
           */
          public PhenomlApiHttpResponse<WorkflowsDeleteResponse> delete(String id,
              RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("workflows")
              .addPathSegment(id);if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new PhenomlApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, WorkflowsDeleteResponse.class), response);
                }
                try {
                  switch (response.code()) {
                    case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new PhenomlApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (IOException e) {
                throw new PhenomlApiException("Network error executing HTTP request", e);
              }
            }

            /**
             * Executes a workflow with provided input data and returns results
             */
            public PhenomlApiHttpResponse<ExecuteWorkflowResponse> execute(String id,
                ExecuteWorkflowRequest request) {
              return execute(id,request,null);
            }

            /**
             * Executes a workflow with provided input data and returns results
             */
            public PhenomlApiHttpResponse<ExecuteWorkflowResponse> execute(String id,
                ExecuteWorkflowRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("workflows")
                .addPathSegment(id)
                .addPathSegments("execute");if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                RequestBody body;
                try {
                  body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                }
                catch(JsonProcessingException e) {
                  throw new PhenomlApiException("Failed to serialize request", e);
                }
                Request okhttpRequest = new Request.Builder()
                  .url(httpUrl.build())
                  .method("POST", body)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Content-Type", "application/json")
                  .addHeader("Accept", "application/json")
                  .build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    return new PhenomlApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ExecuteWorkflowResponse.class), response);
                  }
                  try {
                    switch (response.code()) {
                      case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new PhenomlApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (IOException e) {
                  throw new PhenomlApiException("Network error executing HTTP request", e);
                }
              }
            }
