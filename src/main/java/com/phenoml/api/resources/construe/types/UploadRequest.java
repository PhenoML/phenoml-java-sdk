/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.phenoml.api.resources.construe.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import java.util.Objects;
import java.util.Optional;

public final class UploadRequest {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private UploadRequest(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static UploadRequest csv(UploadRequestCsv value) {
        return new UploadRequest(new CsvValue(value));
    }

    public static UploadRequest json(UploadRequestJson value) {
        return new UploadRequest(new JsonValue(value));
    }

    public boolean isCsv() {
        return value instanceof CsvValue;
    }

    public boolean isJson() {
        return value instanceof JsonValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<UploadRequestCsv> getCsv() {
        if (isCsv()) {
            return Optional.of(((CsvValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<UploadRequestJson> getJson() {
        if (isJson()) {
            return Optional.of(((JsonValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @com.fasterxml.jackson.annotation.JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitCsv(UploadRequestCsv csv);

        T visitJson(UploadRequestJson json);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "format", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(CsvValue.class), @JsonSubTypes.Type(JsonValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("csv")
    @JsonIgnoreProperties("format")
    private static final class CsvValue implements Value {
        @JsonUnwrapped
        private UploadRequestCsv value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CsvValue() {}

        private CsvValue(UploadRequestCsv value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCsv(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CsvValue && equalTo((CsvValue) other);
        }

        private boolean equalTo(CsvValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "UploadRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("json")
    @JsonIgnoreProperties("format")
    private static final class JsonValue implements Value {
        @JsonUnwrapped
        private UploadRequestJson value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private JsonValue() {}

        private JsonValue(UploadRequestJson value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitJson(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof JsonValue && equalTo((JsonValue) other);
        }

        private boolean equalTo(JsonValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "UploadRequest{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("format")
    private static final class _UnknownValue implements Value {
        private String type;

        @com.fasterxml.jackson.annotation.JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "UploadRequest{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
