/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.phenoml.api.resources.fhirprovider.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class FhirProviderAddAuthConfigRequest {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private FhirProviderAddAuthConfigRequest(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static FhirProviderAddAuthConfigRequest jwt(JwtAuth value) {
        return new FhirProviderAddAuthConfigRequest(new JwtValue(value));
    }

    public static FhirProviderAddAuthConfigRequest clientSecret(ClientSecretAuth value) {
        return new FhirProviderAddAuthConfigRequest(new ClientSecretValue(value));
    }

    public static FhirProviderAddAuthConfigRequest onBehalfOf(OnBehalfOfAuth value) {
        return new FhirProviderAddAuthConfigRequest(new OnBehalfOfValue(value));
    }

    public static FhirProviderAddAuthConfigRequest googleHealthcare(GoogleHealthcareAuth value) {
        return new FhirProviderAddAuthConfigRequest(new GoogleHealthcareValue(value));
    }

    public static FhirProviderAddAuthConfigRequest tokenPassthrough(TokenPassthroughAuth value) {
        return new FhirProviderAddAuthConfigRequest(new TokenPassthroughValue(value));
    }

    public static FhirProviderAddAuthConfigRequest none(NoAuth value) {
        return new FhirProviderAddAuthConfigRequest(new NoneValue(value));
    }

    public boolean isJwt() {
        return value instanceof JwtValue;
    }

    public boolean isClientSecret() {
        return value instanceof ClientSecretValue;
    }

    public boolean isOnBehalfOf() {
        return value instanceof OnBehalfOfValue;
    }

    public boolean isGoogleHealthcare() {
        return value instanceof GoogleHealthcareValue;
    }

    public boolean isTokenPassthrough() {
        return value instanceof TokenPassthroughValue;
    }

    public boolean isNone() {
        return value instanceof NoneValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<JwtAuth> getJwt() {
        if (isJwt()) {
            return Optional.of(((JwtValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ClientSecretAuth> getClientSecret() {
        if (isClientSecret()) {
            return Optional.of(((ClientSecretValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<OnBehalfOfAuth> getOnBehalfOf() {
        if (isOnBehalfOf()) {
            return Optional.of(((OnBehalfOfValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<GoogleHealthcareAuth> getGoogleHealthcare() {
        if (isGoogleHealthcare()) {
            return Optional.of(((GoogleHealthcareValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TokenPassthroughAuth> getTokenPassthrough() {
        if (isTokenPassthrough()) {
            return Optional.of(((TokenPassthroughValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<NoAuth> getNone() {
        if (isNone()) {
            return Optional.of(((NoneValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitJwt(JwtAuth jwt);

        T visitClientSecret(ClientSecretAuth clientSecret);

        T visitOnBehalfOf(OnBehalfOfAuth onBehalfOf);

        T visitGoogleHealthcare(GoogleHealthcareAuth googleHealthcare);

        T visitTokenPassthrough(TokenPassthroughAuth tokenPassthrough);

        T visitNone(NoAuth none);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(
            use = JsonTypeInfo.Id.NAME,
            property = "auth_method",
            visible = true,
            defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(JwtValue.class),
        @JsonSubTypes.Type(ClientSecretValue.class),
        @JsonSubTypes.Type(OnBehalfOfValue.class),
        @JsonSubTypes.Type(GoogleHealthcareValue.class),
        @JsonSubTypes.Type(TokenPassthroughValue.class),
        @JsonSubTypes.Type(NoneValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("jwt")
    @JsonIgnoreProperties("auth_method")
    private static final class JwtValue implements Value {
        @JsonUnwrapped
        private JwtAuth value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private JwtValue() {}

        private JwtValue(JwtAuth value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitJwt(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof JwtValue && equalTo((JwtValue) other);
        }

        private boolean equalTo(JwtValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "FhirProviderAddAuthConfigRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("client_secret")
    @JsonIgnoreProperties("auth_method")
    private static final class ClientSecretValue implements Value {
        @JsonUnwrapped
        private ClientSecretAuth value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ClientSecretValue() {}

        private ClientSecretValue(ClientSecretAuth value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitClientSecret(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ClientSecretValue && equalTo((ClientSecretValue) other);
        }

        private boolean equalTo(ClientSecretValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "FhirProviderAddAuthConfigRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("on_behalf_of")
    @JsonIgnoreProperties("auth_method")
    private static final class OnBehalfOfValue implements Value {
        @JsonUnwrapped
        private OnBehalfOfAuth value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private OnBehalfOfValue() {}

        private OnBehalfOfValue(OnBehalfOfAuth value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitOnBehalfOf(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof OnBehalfOfValue && equalTo((OnBehalfOfValue) other);
        }

        private boolean equalTo(OnBehalfOfValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "FhirProviderAddAuthConfigRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("google_healthcare")
    @JsonIgnoreProperties("auth_method")
    private static final class GoogleHealthcareValue implements Value {
        @JsonUnwrapped
        private GoogleHealthcareAuth value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private GoogleHealthcareValue() {}

        private GoogleHealthcareValue(GoogleHealthcareAuth value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitGoogleHealthcare(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof GoogleHealthcareValue && equalTo((GoogleHealthcareValue) other);
        }

        private boolean equalTo(GoogleHealthcareValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "FhirProviderAddAuthConfigRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("token_passthrough")
    @JsonIgnoreProperties("auth_method")
    private static final class TokenPassthroughValue implements Value {
        @JsonUnwrapped
        private TokenPassthroughAuth value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private TokenPassthroughValue() {}

        private TokenPassthroughValue(TokenPassthroughAuth value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTokenPassthrough(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof TokenPassthroughValue && equalTo((TokenPassthroughValue) other);
        }

        private boolean equalTo(TokenPassthroughValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "FhirProviderAddAuthConfigRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("none")
    @JsonIgnoreProperties("auth_method")
    private static final class NoneValue implements Value {
        @JsonUnwrapped
        private NoAuth value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private NoneValue() {}

        private NoneValue(NoAuth value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitNone(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof NoneValue && equalTo((NoneValue) other);
        }

        private boolean equalTo(NoneValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "FhirProviderAddAuthConfigRequest{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("auth_method")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "FhirProviderAddAuthConfigRequest{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
