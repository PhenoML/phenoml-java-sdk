/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.phenoml.api.resources.workflows.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Object;
import java.lang.String;

public final class WorkflowStepSummaryType {
  public static final WorkflowStepSummaryType WORKFLOW = new WorkflowStepSummaryType(Value.WORKFLOW, "workflow");

  public static final WorkflowStepSummaryType SEARCH = new WorkflowStepSummaryType(Value.SEARCH, "search");

  public static final WorkflowStepSummaryType CREATE = new WorkflowStepSummaryType(Value.CREATE, "create");

  public static final WorkflowStepSummaryType DECISION_NODE = new WorkflowStepSummaryType(Value.DECISION_NODE, "decision_node");

  private final Value value;

  private final String string;

  WorkflowStepSummaryType(Value value, String string) {
    this.value = value;
    this.string = string;
  }

  public Value getEnumValue() {
    return value;
  }

  @java.lang.Override
  @JsonValue
  public String toString() {
    return this.string;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    return (this == other) 
      || (other instanceof WorkflowStepSummaryType && this.string.equals(((WorkflowStepSummaryType) other).string));
  }

  @java.lang.Override
  public int hashCode() {
    return this.string.hashCode();
  }

  public <T> T visit(Visitor<T> visitor) {
    switch (value) {
      case WORKFLOW:
        return visitor.visitWorkflow();
      case SEARCH:
        return visitor.visitSearch();
      case CREATE:
        return visitor.visitCreate();
      case DECISION_NODE:
        return visitor.visitDecisionNode();
      case UNKNOWN:
      default:
        return visitor.visitUnknown(string);
    }
  }

  @JsonCreator(
      mode = JsonCreator.Mode.DELEGATING
  )
  public static WorkflowStepSummaryType valueOf(String value) {
    switch (value) {
      case "workflow":
        return WORKFLOW;
      case "search":
        return SEARCH;
      case "create":
        return CREATE;
      case "decision_node":
        return DECISION_NODE;
      default:
        return new WorkflowStepSummaryType(Value.UNKNOWN, value);
    }
  }

  public enum Value {
    SEARCH,

    CREATE,

    WORKFLOW,

    DECISION_NODE,

    UNKNOWN
  }

  public interface Visitor<T> {
    T visitSearch();

    T visitCreate();

    T visitWorkflow();

    T visitDecisionNode();

    T visitUnknown(String unknownType);
  }
}
