/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.phenoml.api.resources.lang2fhirbatch.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.phenoml.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = BatchError.Builder.class)
public final class BatchError {
    private final String kind;

    private final String message;

    private final Map<String, Object> additionalProperties;

    private BatchError(String kind, String message, Map<String, Object> additionalProperties) {
        this.kind = kind;
        this.message = message;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Short stable token to branch on. Item-level kinds: <code>invalid_input</code>
     * (the stored body was not a valid create/document request),
     * <code>processing_failed</code> (the conversion failed), <code>budget_exceeded</code> (the
     * item ran past its time budget — 600s for a document item, 450s for a
     * create item), <code>result_too_large</code> (the result exceeded the storage
     * cap), <code>input_unavailable</code> (the input could not be read), and
     * <code>retries_exhausted</code> / <code>attempts_exhausted</code> (buried after too many
     * failed attempts).
     * Job-level kinds: <code>timeout</code> (the job did not finish within 36 hours
     * of creation).
     */
    @JsonProperty("kind")
    public String getKind() {
        return kind;
    }

    /**
     * @return Human-readable description of the failure.
     */
    @JsonProperty("message")
    public String getMessage() {
        return message;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof BatchError && equalTo((BatchError) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(BatchError other) {
        return kind.equals(other.kind) && message.equals(other.message);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.kind, this.message);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static KindStage builder() {
        return new Builder();
    }

    public interface KindStage {
        /**
         * <p>Short stable token to branch on. Item-level kinds: <code>invalid_input</code>
         * (the stored body was not a valid create/document request),
         * <code>processing_failed</code> (the conversion failed), <code>budget_exceeded</code> (the
         * item ran past its time budget — 600s for a document item, 450s for a
         * create item), <code>result_too_large</code> (the result exceeded the storage
         * cap), <code>input_unavailable</code> (the input could not be read), and
         * <code>retries_exhausted</code> / <code>attempts_exhausted</code> (buried after too many
         * failed attempts).
         * Job-level kinds: <code>timeout</code> (the job did not finish within 36 hours
         * of creation).</p>
         */
        MessageStage kind(@NotNull String kind);

        Builder from(BatchError other);
    }

    public interface MessageStage {
        /**
         * <p>Human-readable description of the failure.</p>
         */
        _FinalStage message(@NotNull String message);
    }

    public interface _FinalStage {
        BatchError build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements KindStage, MessageStage, _FinalStage {
        private String kind;

        private String message;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(BatchError other) {
            kind(other.getKind());
            message(other.getMessage());
            return this;
        }

        /**
         * <p>Short stable token to branch on. Item-level kinds: <code>invalid_input</code>
         * (the stored body was not a valid create/document request),
         * <code>processing_failed</code> (the conversion failed), <code>budget_exceeded</code> (the
         * item ran past its time budget — 600s for a document item, 450s for a
         * create item), <code>result_too_large</code> (the result exceeded the storage
         * cap), <code>input_unavailable</code> (the input could not be read), and
         * <code>retries_exhausted</code> / <code>attempts_exhausted</code> (buried after too many
         * failed attempts).
         * Job-level kinds: <code>timeout</code> (the job did not finish within 36 hours
         * of creation).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("kind")
        public MessageStage kind(@NotNull String kind) {
            this.kind = Objects.requireNonNull(kind, "kind must not be null");
            return this;
        }

        /**
         * <p>Human-readable description of the failure.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("message")
        public _FinalStage message(@NotNull String message) {
            this.message = Objects.requireNonNull(message, "message must not be null");
            return this;
        }

        @java.lang.Override
        public BatchError build() {
            return new BatchError(kind, message, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
