/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.phenoml.api.resources.lang2fhirbatch;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.phenoml.api.core.ClientOptions;
import com.phenoml.api.core.FileStream;
import com.phenoml.api.core.MediaTypes;
import com.phenoml.api.core.ObjectMappers;
import com.phenoml.api.core.PhenomlClientApiException;
import com.phenoml.api.core.PhenomlClientException;
import com.phenoml.api.core.PhenomlClientHttpResponse;
import com.phenoml.api.core.QueryStringMapper;
import com.phenoml.api.core.RequestOptions;
import com.phenoml.api.core.RetryInterceptor;
import com.phenoml.api.resources.lang2fhirbatch.errors.BadRequestError;
import com.phenoml.api.resources.lang2fhirbatch.errors.ClientClosedRequestError;
import com.phenoml.api.resources.lang2fhirbatch.errors.ConflictError;
import com.phenoml.api.resources.lang2fhirbatch.errors.ContentTooLargeError;
import com.phenoml.api.resources.lang2fhirbatch.errors.GatewayTimeoutError;
import com.phenoml.api.resources.lang2fhirbatch.errors.InternalServerError;
import com.phenoml.api.resources.lang2fhirbatch.errors.NotFoundError;
import com.phenoml.api.resources.lang2fhirbatch.errors.UnauthorizedError;
import com.phenoml.api.resources.lang2fhirbatch.requests.CreateBatchRequest;
import com.phenoml.api.resources.lang2fhirbatch.requests.GetRequest;
import com.phenoml.api.resources.lang2fhirbatch.requests.GetResultsRequest;
import com.phenoml.api.resources.lang2fhirbatch.requests.ListRequest;
import com.phenoml.api.resources.lang2fhirbatch.requests.UploadItemRequest;
import com.phenoml.api.resources.lang2fhirbatch.types.BatchJob;
import com.phenoml.api.resources.lang2fhirbatch.types.JobDetailResponse;
import com.phenoml.api.resources.lang2fhirbatch.types.JobListResponse;
import com.phenoml.api.resources.lang2fhirbatch.types.ResultsPageResponse;
import com.phenoml.api.resources.lang2fhirbatch.types.UploadItemResponse;
import java.io.File;
import java.io.IOException;
import java.io.InputStream;
import java.nio.file.Files;
import java.util.Map;
import java.util.Optional;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.MediaType;
import okhttp3.MultipartBody;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawLang2FhirBatchClient {
    protected final ClientOptions clientOptions;

    public AsyncRawLang2FhirBatchClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns a page of the instance's batch jobs, newest first, without
     * per-job counts. Jobs are shared across the instance's credentials, so
     * this lists every batch job on the instance, not just the calling
     * credential's.
     */
    public CompletableFuture<PhenomlClientHttpResponse<JobListResponse>> list() {
        return list(ListRequest.builder().build());
    }

    /**
     * Returns a page of the instance's batch jobs, newest first, without
     * per-job counts. Jobs are shared across the instance's credentials, so
     * this lists every batch job on the instance, not just the calling
     * credential's.
     */
    public CompletableFuture<PhenomlClientHttpResponse<JobListResponse>> list(RequestOptions requestOptions) {
        return list(ListRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a page of the instance's batch jobs, newest first, without
     * per-job counts. Jobs are shared across the instance's credentials, so
     * this lists every batch job on the instance, not just the calling
     * credential's.
     */
    public CompletableFuture<PhenomlClientHttpResponse<JobListResponse>> list(ListRequest request) {
        return list(request, null);
    }

    /**
     * Returns a page of the instance's batch jobs, newest first, without
     * per-job counts. Jobs are shared across the instance's credentials, so
     * this lists every batch job on the instance, not just the calling
     * credential's.
     */
    public CompletableFuture<PhenomlClientHttpResponse<JobListResponse>> list(
            ListRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("lang2fhir/batch");
        if (request.getCursor().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "cursor", request.getCursor().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<PhenomlClientHttpResponse<JobListResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PhenomlClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, JobListResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 499:
                                future.completeExceptionally(new ClientClosedRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 504:
                                future.completeExceptionally(new GatewayTimeoutError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PhenomlClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new PhenomlClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Opens an empty batch job. Items arrive on later upload calls and the set
     * is sealed at finalize.
     * <p>Supplying <code>request_id</code> makes the create idempotent on that token: a
     * retried submit whose response was lost returns the original job rather
     * than opening a second one. This dedupe is scoped to the calling
     * credential.</p>
     * <p>An instance may hold at most 4 active (pending or processing) jobs at
     * once; a create past that limit returns <code>409</code>. The limit is instance-wide
     * — jobs are shared across the instance's credentials — so another
     * credential's jobs count against it.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<BatchJob>> create() {
        return create(CreateBatchRequest.builder().build());
    }

    /**
     * Opens an empty batch job. Items arrive on later upload calls and the set
     * is sealed at finalize.
     * <p>Supplying <code>request_id</code> makes the create idempotent on that token: a
     * retried submit whose response was lost returns the original job rather
     * than opening a second one. This dedupe is scoped to the calling
     * credential.</p>
     * <p>An instance may hold at most 4 active (pending or processing) jobs at
     * once; a create past that limit returns <code>409</code>. The limit is instance-wide
     * — jobs are shared across the instance's credentials — so another
     * credential's jobs count against it.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<BatchJob>> create(RequestOptions requestOptions) {
        return create(CreateBatchRequest.builder().build(), requestOptions);
    }

    /**
     * Opens an empty batch job. Items arrive on later upload calls and the set
     * is sealed at finalize.
     * <p>Supplying <code>request_id</code> makes the create idempotent on that token: a
     * retried submit whose response was lost returns the original job rather
     * than opening a second one. This dedupe is scoped to the calling
     * credential.</p>
     * <p>An instance may hold at most 4 active (pending or processing) jobs at
     * once; a create past that limit returns <code>409</code>. The limit is instance-wide
     * — jobs are shared across the instance's credentials — so another
     * credential's jobs count against it.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<BatchJob>> create(CreateBatchRequest request) {
        return create(request, null);
    }

    /**
     * Opens an empty batch job. Items arrive on later upload calls and the set
     * is sealed at finalize.
     * <p>Supplying <code>request_id</code> makes the create idempotent on that token: a
     * retried submit whose response was lost returns the original job rather
     * than opening a second one. This dedupe is scoped to the calling
     * credential.</p>
     * <p>An instance may hold at most 4 active (pending or processing) jobs at
     * once; a create past that limit returns <code>409</code>. The limit is instance-wide
     * — jobs are shared across the instance's credentials — so another
     * credential's jobs count against it.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<BatchJob>> create(
            CreateBatchRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("lang2fhir/batch");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PhenomlClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<PhenomlClientHttpResponse<BatchJob>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PhenomlClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BatchJob.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 499:
                                future.completeExceptionally(new ClientClosedRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 504:
                                future.completeExceptionally(new GatewayTimeoutError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PhenomlClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new PhenomlClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Stores one item of a job from a multipart upload. A batch's items arrive
     * one per request. The item carries <strong>either</strong> a <code>document</code> extraction
     * (whose input file rides as raw bytes in the <code>file</code> part) <strong>or</strong> a
     * <code>create</code> extraction (JSON only, no file).
     * <p>The upload enforces these rules:</p>
     * <ul>
     * <li>Set <strong>exactly one</strong> of <code>document</code> or <code>create</code>. Setting both, or
     * neither, is a <code>400</code>.</li>
     * <li>When <code>document</code> is set, <code>file</code> is <strong>required</strong> — it supplies the
     * document's binary content (PDF or image).</li>
     * <li>When <code>create</code> is set, <code>file</code> is <strong>forbidden</strong> — a create item carries
     * no file.</li>
     * <li><code>document</code> and <code>create</code> must each be a JSON <strong>object</strong>.</li>
     * </ul>
     * <p>Only the item's structure is checked here: the fields inside <code>document</code>
     * or <code>create</code> are not validated at upload. A body that is well-formed JSON
     * but not a valid request for its endpoint is still accepted with <code>202</code>
     * and fails later during processing, recorded as an item <code>error</code>. A
     * wrong-typed field the endpoint cannot decode fails as <code>invalid_input</code>; a
     * body that decodes but the pipeline rejects (for example, a missing
     * required field) fails as <code>processing_failed</code>.</p>
     * <p>Supplying <code>request_id</code> makes the upload idempotent on that token. A
     * re-upload under the same token overwrites the same item rather than
     * adding a second, so a client that lost an upload's response can safely
     * re-send it. The response's <code>deduplicated</code> is <code>true</code> only when the
     * re-uploaded payload matches the one already stored; a same-token upload
     * with a changed payload overwrites in place and returns <code>false</code>.</p>
     * <p>Set a <code>request_id</code> on <strong>every</strong> upload: re-sending under the same token
     * is the only way to repair a lost or incomplete upload, including the one
     * a finalize <code>409</code> reports. Without one, a re-send adds a new item instead
     * of replacing the missing one, and the job cannot be finalized.</p>
     * <p>Uploads are rejected once the job has been finalized (<code>409</code>), once it
     * holds its 500-item limit (<code>409</code>), or when the item is too large (<code>413</code> —
     * see the raw-file limit in the API description).</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<UploadItemResponse>> uploadItem(
            String jobId, Optional<File> file) {
        return uploadItem(jobId, file, UploadItemRequest.builder().build());
    }

    /**
     * Stores one item of a job from a multipart upload. A batch's items arrive
     * one per request. The item carries <strong>either</strong> a <code>document</code> extraction
     * (whose input file rides as raw bytes in the <code>file</code> part) <strong>or</strong> a
     * <code>create</code> extraction (JSON only, no file).
     * <p>The upload enforces these rules:</p>
     * <ul>
     * <li>Set <strong>exactly one</strong> of <code>document</code> or <code>create</code>. Setting both, or
     * neither, is a <code>400</code>.</li>
     * <li>When <code>document</code> is set, <code>file</code> is <strong>required</strong> — it supplies the
     * document's binary content (PDF or image).</li>
     * <li>When <code>create</code> is set, <code>file</code> is <strong>forbidden</strong> — a create item carries
     * no file.</li>
     * <li><code>document</code> and <code>create</code> must each be a JSON <strong>object</strong>.</li>
     * </ul>
     * <p>Only the item's structure is checked here: the fields inside <code>document</code>
     * or <code>create</code> are not validated at upload. A body that is well-formed JSON
     * but not a valid request for its endpoint is still accepted with <code>202</code>
     * and fails later during processing, recorded as an item <code>error</code>. A
     * wrong-typed field the endpoint cannot decode fails as <code>invalid_input</code>; a
     * body that decodes but the pipeline rejects (for example, a missing
     * required field) fails as <code>processing_failed</code>.</p>
     * <p>Supplying <code>request_id</code> makes the upload idempotent on that token. A
     * re-upload under the same token overwrites the same item rather than
     * adding a second, so a client that lost an upload's response can safely
     * re-send it. The response's <code>deduplicated</code> is <code>true</code> only when the
     * re-uploaded payload matches the one already stored; a same-token upload
     * with a changed payload overwrites in place and returns <code>false</code>.</p>
     * <p>Set a <code>request_id</code> on <strong>every</strong> upload: re-sending under the same token
     * is the only way to repair a lost or incomplete upload, including the one
     * a finalize <code>409</code> reports. Without one, a re-send adds a new item instead
     * of replacing the missing one, and the job cannot be finalized.</p>
     * <p>Uploads are rejected once the job has been finalized (<code>409</code>), once it
     * holds its 500-item limit (<code>409</code>), or when the item is too large (<code>413</code> —
     * see the raw-file limit in the API description).</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<UploadItemResponse>> uploadItem(
            String jobId, Optional<File> file, RequestOptions requestOptions) {
        return uploadItem(jobId, file, UploadItemRequest.builder().build(), requestOptions);
    }

    /**
     * Stores one item of a job from a multipart upload. A batch's items arrive
     * one per request. The item carries <strong>either</strong> a <code>document</code> extraction
     * (whose input file rides as raw bytes in the <code>file</code> part) <strong>or</strong> a
     * <code>create</code> extraction (JSON only, no file).
     * <p>The upload enforces these rules:</p>
     * <ul>
     * <li>Set <strong>exactly one</strong> of <code>document</code> or <code>create</code>. Setting both, or
     * neither, is a <code>400</code>.</li>
     * <li>When <code>document</code> is set, <code>file</code> is <strong>required</strong> — it supplies the
     * document's binary content (PDF or image).</li>
     * <li>When <code>create</code> is set, <code>file</code> is <strong>forbidden</strong> — a create item carries
     * no file.</li>
     * <li><code>document</code> and <code>create</code> must each be a JSON <strong>object</strong>.</li>
     * </ul>
     * <p>Only the item's structure is checked here: the fields inside <code>document</code>
     * or <code>create</code> are not validated at upload. A body that is well-formed JSON
     * but not a valid request for its endpoint is still accepted with <code>202</code>
     * and fails later during processing, recorded as an item <code>error</code>. A
     * wrong-typed field the endpoint cannot decode fails as <code>invalid_input</code>; a
     * body that decodes but the pipeline rejects (for example, a missing
     * required field) fails as <code>processing_failed</code>.</p>
     * <p>Supplying <code>request_id</code> makes the upload idempotent on that token. A
     * re-upload under the same token overwrites the same item rather than
     * adding a second, so a client that lost an upload's response can safely
     * re-send it. The response's <code>deduplicated</code> is <code>true</code> only when the
     * re-uploaded payload matches the one already stored; a same-token upload
     * with a changed payload overwrites in place and returns <code>false</code>.</p>
     * <p>Set a <code>request_id</code> on <strong>every</strong> upload: re-sending under the same token
     * is the only way to repair a lost or incomplete upload, including the one
     * a finalize <code>409</code> reports. Without one, a re-send adds a new item instead
     * of replacing the missing one, and the job cannot be finalized.</p>
     * <p>Uploads are rejected once the job has been finalized (<code>409</code>), once it
     * holds its 500-item limit (<code>409</code>), or when the item is too large (<code>413</code> —
     * see the raw-file limit in the API description).</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<UploadItemResponse>> uploadItem(
            String jobId, Optional<File> file, UploadItemRequest request) {
        return uploadItem(jobId, file, request, null);
    }

    /**
     * Stores one item of a job from a multipart upload. A batch's items arrive
     * one per request. The item carries <strong>either</strong> a <code>document</code> extraction
     * (whose input file rides as raw bytes in the <code>file</code> part) <strong>or</strong> a
     * <code>create</code> extraction (JSON only, no file).
     * <p>The upload enforces these rules:</p>
     * <ul>
     * <li>Set <strong>exactly one</strong> of <code>document</code> or <code>create</code>. Setting both, or
     * neither, is a <code>400</code>.</li>
     * <li>When <code>document</code> is set, <code>file</code> is <strong>required</strong> — it supplies the
     * document's binary content (PDF or image).</li>
     * <li>When <code>create</code> is set, <code>file</code> is <strong>forbidden</strong> — a create item carries
     * no file.</li>
     * <li><code>document</code> and <code>create</code> must each be a JSON <strong>object</strong>.</li>
     * </ul>
     * <p>Only the item's structure is checked here: the fields inside <code>document</code>
     * or <code>create</code> are not validated at upload. A body that is well-formed JSON
     * but not a valid request for its endpoint is still accepted with <code>202</code>
     * and fails later during processing, recorded as an item <code>error</code>. A
     * wrong-typed field the endpoint cannot decode fails as <code>invalid_input</code>; a
     * body that decodes but the pipeline rejects (for example, a missing
     * required field) fails as <code>processing_failed</code>.</p>
     * <p>Supplying <code>request_id</code> makes the upload idempotent on that token. A
     * re-upload under the same token overwrites the same item rather than
     * adding a second, so a client that lost an upload's response can safely
     * re-send it. The response's <code>deduplicated</code> is <code>true</code> only when the
     * re-uploaded payload matches the one already stored; a same-token upload
     * with a changed payload overwrites in place and returns <code>false</code>.</p>
     * <p>Set a <code>request_id</code> on <strong>every</strong> upload: re-sending under the same token
     * is the only way to repair a lost or incomplete upload, including the one
     * a finalize <code>409</code> reports. Without one, a re-send adds a new item instead
     * of replacing the missing one, and the job cannot be finalized.</p>
     * <p>Uploads are rejected once the job has been finalized (<code>409</code>), once it
     * holds its 500-item limit (<code>409</code>), or when the item is too large (<code>413</code> —
     * see the raw-file limit in the API description).</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<UploadItemResponse>> uploadItem(
            String jobId, Optional<File> file, UploadItemRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("lang2fhir/batch")
                .addPathSegment(jobId)
                .addPathSegments("items");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
        try {
            if (request.getDocument().isPresent()) {
                multipartBodyBuilder.addFormDataPart(
                        "document",
                        ObjectMappers.JSON_MAPPER.writeValueAsString(
                                request.getDocument().get()));
            }
            if (request.getCreate().isPresent()) {
                multipartBodyBuilder.addFormDataPart(
                        "create",
                        ObjectMappers.JSON_MAPPER.writeValueAsString(
                                request.getCreate().get()));
            }
            if (file.isPresent()) {
                String fileMimeType = Files.probeContentType(file.get().toPath());
                MediaType fileMimeTypeMediaType = fileMimeType != null ? MediaType.parse(fileMimeType) : null;
                multipartBodyBuilder.addFormDataPart(
                        "file", file.get().getName(), RequestBody.create(file.get(), fileMimeTypeMediaType));
            }
            if (request.getRequestId().isPresent()) {
                multipartBodyBuilder.addFormDataPart(
                        "request_id", request.getRequestId().get());
            }
            if (request.getId().isPresent()) {
                multipartBodyBuilder.addFormDataPart("id", request.getId().get());
            }
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", multipartBodyBuilder.build())
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<PhenomlClientHttpResponse<UploadItemResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PhenomlClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UploadItemResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 413:
                                future.completeExceptionally(new ContentTooLargeError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 499:
                                future.completeExceptionally(new ClientClosedRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 504:
                                future.completeExceptionally(new GatewayTimeoutError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PhenomlClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new PhenomlClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<PhenomlClientHttpResponse<UploadItemResponse>> uploadItem(
            String jobId, Optional<File> file, InputStream stream, String filename) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("lang2fhir/batch")
                .addPathSegment(jobId)
                .addPathSegments("items");
        FileStream fs = new FileStream(stream, filename, null);
        MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
        multipartBodyBuilder.addFormDataPart("file", filename, fs.toRequestBody());
        RequestBody body = multipartBodyBuilder.build();
        Request.Builder _requestBuilder = new Request.Builder();
        _requestBuilder.url(httpUrl.build());
        _requestBuilder.method("POST", body);
        _requestBuilder.headers(Headers.of(this.clientOptions.headers((RequestOptions) null)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        CompletableFuture<PhenomlClientHttpResponse<UploadItemResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PhenomlClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UploadItemResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 413:
                                future.completeExceptionally(new ContentTooLargeError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 499:
                                future.completeExceptionally(new ClientClosedRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 504:
                                future.completeExceptionally(new GatewayTimeoutError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PhenomlClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new PhenomlClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<PhenomlClientHttpResponse<UploadItemResponse>> uploadItem(
            String jobId, Optional<File> file, InputStream stream, String filename, MediaType mediaType) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("lang2fhir/batch")
                .addPathSegment(jobId)
                .addPathSegments("items");
        FileStream fs = new FileStream(stream, filename, mediaType);
        MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
        multipartBodyBuilder.addFormDataPart("file", filename, fs.toRequestBody());
        RequestBody body = multipartBodyBuilder.build();
        Request.Builder _requestBuilder = new Request.Builder();
        _requestBuilder.url(httpUrl.build());
        _requestBuilder.method("POST", body);
        _requestBuilder.headers(Headers.of(this.clientOptions.headers((RequestOptions) null)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        CompletableFuture<PhenomlClientHttpResponse<UploadItemResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PhenomlClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UploadItemResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 413:
                                future.completeExceptionally(new ContentTooLargeError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 499:
                                future.completeExceptionally(new ClientClosedRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 504:
                                future.completeExceptionally(new GatewayTimeoutError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PhenomlClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new PhenomlClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<PhenomlClientHttpResponse<UploadItemResponse>> uploadItem(
            String jobId, Optional<File> file, InputStream stream, String filename, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("lang2fhir/batch")
                .addPathSegment(jobId)
                .addPathSegments("items");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        FileStream fs = new FileStream(stream, filename, null);
        MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
        multipartBodyBuilder.addFormDataPart("file", filename, fs.toRequestBody());
        RequestBody body = multipartBodyBuilder.build();
        Request.Builder _requestBuilder = new Request.Builder();
        _requestBuilder.url(httpUrl.build());
        _requestBuilder.method("POST", body);
        _requestBuilder.headers(Headers.of(this.clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<PhenomlClientHttpResponse<UploadItemResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PhenomlClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UploadItemResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 413:
                                future.completeExceptionally(new ContentTooLargeError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 499:
                                future.completeExceptionally(new ClientClosedRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 504:
                                future.completeExceptionally(new GatewayTimeoutError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PhenomlClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new PhenomlClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<PhenomlClientHttpResponse<UploadItemResponse>> uploadItem(
            String jobId,
            Optional<File> file,
            InputStream stream,
            String filename,
            MediaType mediaType,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("lang2fhir/batch")
                .addPathSegment(jobId)
                .addPathSegments("items");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        FileStream fs = new FileStream(stream, filename, mediaType);
        MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
        multipartBodyBuilder.addFormDataPart("file", filename, fs.toRequestBody());
        RequestBody body = multipartBodyBuilder.build();
        Request.Builder _requestBuilder = new Request.Builder();
        _requestBuilder.url(httpUrl.build());
        _requestBuilder.method("POST", body);
        _requestBuilder.headers(Headers.of(this.clientOptions.headers(requestOptions)));
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<PhenomlClientHttpResponse<UploadItemResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PhenomlClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UploadItemResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 413:
                                future.completeExceptionally(new ContentTooLargeError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 499:
                                future.completeExceptionally(new ClientClosedRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 504:
                                future.completeExceptionally(new GatewayTimeoutError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PhenomlClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new PhenomlClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Seals the job's item set and starts processing. Takes no request body.
     * Finalize is idempotent: a retried finalize succeeds again.
     * <p>If a previous upload did not complete, finalize returns a <code>409</code>; re-send
     * the missing upload (with the same <code>request_id</code>), then finalize.
     * Finalizing a job with no items is a <code>400</code>.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<BatchJob>> finalize(String jobId) {
        return finalize(jobId, null);
    }

    /**
     * Seals the job's item set and starts processing. Takes no request body.
     * Finalize is idempotent: a retried finalize succeeds again.
     * <p>If a previous upload did not complete, finalize returns a <code>409</code>; re-send
     * the missing upload (with the same <code>request_id</code>), then finalize.
     * Finalizing a job with no items is a <code>400</code>.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<BatchJob>> finalize(
            String jobId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("lang2fhir/batch")
                .addPathSegment(jobId)
                .addPathSegments("finalize");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<PhenomlClientHttpResponse<BatchJob>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PhenomlClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BatchJob.class), response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 499:
                                future.completeExceptionally(new ClientClosedRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 504:
                                future.completeExceptionally(new GatewayTimeoutError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PhenomlClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new PhenomlClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns a job's record, its per-status item counts, and one page of
     * per-item statuses.
     * <p>Items are listed in a stable order that is not upload order and is the
     * same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<JobDetailResponse>> get(String jobId) {
        return get(jobId, GetRequest.builder().build());
    }

    /**
     * Returns a job's record, its per-status item counts, and one page of
     * per-item statuses.
     * <p>Items are listed in a stable order that is not upload order and is the
     * same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<JobDetailResponse>> get(
            String jobId, RequestOptions requestOptions) {
        return get(jobId, GetRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a job's record, its per-status item counts, and one page of
     * per-item statuses.
     * <p>Items are listed in a stable order that is not upload order and is the
     * same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<JobDetailResponse>> get(String jobId, GetRequest request) {
        return get(jobId, request, null);
    }

    /**
     * Returns a job's record, its per-status item counts, and one page of
     * per-item statuses.
     * <p>Items are listed in a stable order that is not upload order and is the
     * same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<JobDetailResponse>> get(
            String jobId, GetRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("lang2fhir/batch")
                .addPathSegment(jobId);
        if (request.getCursor().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "cursor", request.getCursor().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<PhenomlClientHttpResponse<JobDetailResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PhenomlClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, JobDetailResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 499:
                                future.completeExceptionally(new ClientClosedRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 504:
                                future.completeExceptionally(new GatewayTimeoutError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PhenomlClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new PhenomlClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * A lighter status page. Returns the same per-item status entries as
     * <code>GET /lang2fhir/batch/{job_id}</code>, but without the job record or counts,
     * and the entries carry <code>result_size</code> rather than any result content. Use
     * each entry's <code>item_id</code> to fetch that item's result from
     * <code>GET /lang2fhir/batch/{job_id}/results/{item_id}</code>.
     * <p>Entries are listed in a stable order that is not upload order and is
     * the same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<ResultsPageResponse>> getResults(String jobId) {
        return getResults(jobId, GetResultsRequest.builder().build());
    }

    /**
     * A lighter status page. Returns the same per-item status entries as
     * <code>GET /lang2fhir/batch/{job_id}</code>, but without the job record or counts,
     * and the entries carry <code>result_size</code> rather than any result content. Use
     * each entry's <code>item_id</code> to fetch that item's result from
     * <code>GET /lang2fhir/batch/{job_id}/results/{item_id}</code>.
     * <p>Entries are listed in a stable order that is not upload order and is
     * the same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<ResultsPageResponse>> getResults(
            String jobId, RequestOptions requestOptions) {
        return getResults(jobId, GetResultsRequest.builder().build(), requestOptions);
    }

    /**
     * A lighter status page. Returns the same per-item status entries as
     * <code>GET /lang2fhir/batch/{job_id}</code>, but without the job record or counts,
     * and the entries carry <code>result_size</code> rather than any result content. Use
     * each entry's <code>item_id</code> to fetch that item's result from
     * <code>GET /lang2fhir/batch/{job_id}/results/{item_id}</code>.
     * <p>Entries are listed in a stable order that is not upload order and is
     * the same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<ResultsPageResponse>> getResults(
            String jobId, GetResultsRequest request) {
        return getResults(jobId, request, null);
    }

    /**
     * A lighter status page. Returns the same per-item status entries as
     * <code>GET /lang2fhir/batch/{job_id}</code>, but without the job record or counts,
     * and the entries carry <code>result_size</code> rather than any result content. Use
     * each entry's <code>item_id</code> to fetch that item's result from
     * <code>GET /lang2fhir/batch/{job_id}/results/{item_id}</code>.
     * <p>Entries are listed in a stable order that is not upload order and is
     * the same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<ResultsPageResponse>> getResults(
            String jobId, GetResultsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("lang2fhir/batch")
                .addPathSegment(jobId)
                .addPathSegments("results");
        if (request.getCursor().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "cursor", request.getCursor().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<PhenomlClientHttpResponse<ResultsPageResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PhenomlClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ResultsPageResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 499:
                                future.completeExceptionally(new ClientClosedRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 504:
                                future.completeExceptionally(new GatewayTimeoutError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PhenomlClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new PhenomlClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Streams one item's stored result bytes verbatim as <code>application/json</code>.
     * The body is the response the item's synchronous multi endpoint would have
     * returned — a <code>DocumentMultiResponse</code> for a document item or a
     * <code>CreateMultiResponse</code> for a create item.
     * <p>Only a succeeded item has a result: an item that has not succeeded
     * (pending, processing, or failed) is a <code>409</code>, and a result that has
     * expired is a <code>404</code>.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<Map<String, Object>>> getResult(String jobId, String itemId) {
        return getResult(jobId, itemId, null);
    }

    /**
     * Streams one item's stored result bytes verbatim as <code>application/json</code>.
     * The body is the response the item's synchronous multi endpoint would have
     * returned — a <code>DocumentMultiResponse</code> for a document item or a
     * <code>CreateMultiResponse</code> for a create item.
     * <p>Only a succeeded item has a result: an item that has not succeeded
     * (pending, processing, or failed) is a <code>409</code>, and a result that has
     * expired is a <code>404</code>.</p>
     */
    public CompletableFuture<PhenomlClientHttpResponse<Map<String, Object>>> getResult(
            String jobId, String itemId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("lang2fhir/batch")
                .addPathSegment(jobId)
                .addPathSegments("results")
                .addPathSegment(itemId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<PhenomlClientHttpResponse<Map<String, Object>>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PhenomlClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, new TypeReference<Map<String, Object>>() {}),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 499:
                                future.completeExceptionally(new ClientClosedRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 504:
                                future.completeExceptionally(new GatewayTimeoutError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PhenomlClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new PhenomlClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PhenomlClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
