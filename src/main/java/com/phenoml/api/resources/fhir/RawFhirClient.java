/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.phenoml.api.resources.fhir;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.phenoml.api.core.ClientOptions;
import com.phenoml.api.core.ObjectMappers;
import com.phenoml.api.core.PhenoMLApiException;
import com.phenoml.api.core.PhenoMLException;
import com.phenoml.api.core.PhenoMLHttpResponse;
import com.phenoml.api.core.QueryStringMapper;
import com.phenoml.api.core.RequestOptions;
import com.phenoml.api.resources.fhir.errors.BadGatewayError;
import com.phenoml.api.resources.fhir.errors.BadRequestError;
import com.phenoml.api.resources.fhir.errors.InternalServerError;
import com.phenoml.api.resources.fhir.errors.NotFoundError;
import com.phenoml.api.resources.fhir.errors.UnauthorizedError;
import com.phenoml.api.resources.fhir.requests.FhirCreateRequest;
import com.phenoml.api.resources.fhir.requests.FhirDeleteRequest;
import com.phenoml.api.resources.fhir.requests.FhirExecuteBundleRequest;
import com.phenoml.api.resources.fhir.requests.FhirPatchRequest;
import com.phenoml.api.resources.fhir.requests.FhirSearchRequest;
import com.phenoml.api.resources.fhir.requests.FhirUpsertRequest;
import com.phenoml.api.resources.fhir.types.ErrorResponse;
import com.phenoml.api.resources.fhir.types.FhirBundle;
import com.phenoml.api.resources.fhir.types.FhirPatchRequestBodyItem;
import com.phenoml.api.resources.fhir.types.FhirResource;
import com.phenoml.api.resources.fhir.types.FhirSearchResponse;
import java.io.IOException;
import java.lang.Exception;
import java.lang.Object;
import java.lang.RuntimeException;
import java.lang.String;
import java.util.List;
import java.util.Map;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.MediaType;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawFhirClient {
  protected final ClientOptions clientOptions;

  public RawFhirClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Retrieves FHIR resources from the specified provider. Supports both individual resource retrieval and search operations based on the FHIR path and query parameters.
   * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
   */
  public PhenoMLHttpResponse<FhirSearchResponse> search(String fhirProviderId, String fhirPath) {
    return search(fhirProviderId,fhirPath,FhirSearchRequest.builder().build());
  }

  /**
   * Retrieves FHIR resources from the specified provider. Supports both individual resource retrieval and search operations based on the FHIR path and query parameters.
   * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
   */
  public PhenoMLHttpResponse<FhirSearchResponse> search(String fhirProviderId, String fhirPath,
      RequestOptions requestOptions) {
    return search(fhirProviderId,fhirPath,FhirSearchRequest.builder().build(),requestOptions);
  }

  /**
   * Retrieves FHIR resources from the specified provider. Supports both individual resource retrieval and search operations based on the FHIR path and query parameters.
   * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
   */
  public PhenoMLHttpResponse<FhirSearchResponse> search(String fhirProviderId, String fhirPath,
      FhirSearchRequest request) {
    return search(fhirProviderId,fhirPath,request,null);
  }

  /**
   * Retrieves FHIR resources from the specified provider. Supports both individual resource retrieval and search operations based on the FHIR path and query parameters.
   * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
   */
  public PhenoMLHttpResponse<FhirSearchResponse> search(String fhirProviderId, String fhirPath,
      FhirSearchRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("fhir-provider")
      .addPathSegment(fhirProviderId)
      .addPathSegments("fhir")
      .addPathSegment(fhirPath);if (request.getQueryParameters().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "query_parameters", request.getQueryParameters().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      if (request.getPhenomlOnBehalfOf().isPresent()) {
        _requestBuilder.addHeader("X-Phenoml-On-Behalf-Of", request.getPhenomlOnBehalfOf().get());
      }
      if (request.getPhenomlFhirProvider().isPresent()) {
        _requestBuilder.addHeader("X-Phenoml-Fhir-Provider", request.getPhenomlFhirProvider().get());
      }
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new PhenoMLHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FhirSearchResponse.class), response);
        }
        try {
          switch (response.code()) {
            case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new PhenoMLApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new PhenoMLException("Network error executing HTTP request", e);
      }
    }

    /**
     * Creates a new FHIR resource on the specified provider. The request body should contain a valid FHIR resource in JSON format.
     * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
     */
    public PhenoMLHttpResponse<FhirResource> create(String fhirProviderId, String fhirPath,
        FhirResource body) {
      return create(fhirProviderId, fhirPath, FhirCreateRequest.builder().body(body).build());
    }

    /**
     * Creates a new FHIR resource on the specified provider. The request body should contain a valid FHIR resource in JSON format.
     * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
     */
    public PhenoMLHttpResponse<FhirResource> create(String fhirProviderId, String fhirPath,
        FhirResource body, RequestOptions requestOptions) {
      return create(fhirProviderId, fhirPath, FhirCreateRequest.builder().body(body).build(), requestOptions);
    }

    /**
     * Creates a new FHIR resource on the specified provider. The request body should contain a valid FHIR resource in JSON format.
     * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
     */
    public PhenoMLHttpResponse<FhirResource> create(String fhirProviderId, String fhirPath,
        FhirCreateRequest request) {
      return create(fhirProviderId,fhirPath,request,null);
    }

    /**
     * Creates a new FHIR resource on the specified provider. The request body should contain a valid FHIR resource in JSON format.
     * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
     */
    public PhenoMLHttpResponse<FhirResource> create(String fhirProviderId, String fhirPath,
        FhirCreateRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("fhir-provider")
        .addPathSegment(fhirProviderId)
        .addPathSegments("fhir")
        .addPathSegment(fhirPath);if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaType.parse("application/fhir+json"));
        }
        catch(Exception e) {
          throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/fhir+json")
          .addHeader("Accept", "application/json");
        if (request.getPhenomlOnBehalfOf().isPresent()) {
          _requestBuilder.addHeader("X-Phenoml-On-Behalf-Of", request.getPhenomlOnBehalfOf().get());
        }
        if (request.getPhenomlFhirProvider().isPresent()) {
          _requestBuilder.addHeader("X-Phenoml-Fhir-Provider", request.getPhenomlFhirProvider().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new PhenoMLHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FhirResource.class), response);
          }
          try {
            switch (response.code()) {
              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new PhenoMLApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new PhenoMLException("Network error executing HTTP request", e);
        }
      }

      /**
       * Creates or updates a FHIR resource on the specified provider. If the resource exists, it will be updated; otherwise, it will be created.
       * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
       */
      public PhenoMLHttpResponse<FhirResource> upsert(String fhirProviderId, String fhirPath,
          FhirResource body) {
        return upsert(fhirProviderId, fhirPath, FhirUpsertRequest.builder().body(body).build());
      }

      /**
       * Creates or updates a FHIR resource on the specified provider. If the resource exists, it will be updated; otherwise, it will be created.
       * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
       */
      public PhenoMLHttpResponse<FhirResource> upsert(String fhirProviderId, String fhirPath,
          FhirResource body, RequestOptions requestOptions) {
        return upsert(fhirProviderId, fhirPath, FhirUpsertRequest.builder().body(body).build(), requestOptions);
      }

      /**
       * Creates or updates a FHIR resource on the specified provider. If the resource exists, it will be updated; otherwise, it will be created.
       * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
       */
      public PhenoMLHttpResponse<FhirResource> upsert(String fhirProviderId, String fhirPath,
          FhirUpsertRequest request) {
        return upsert(fhirProviderId,fhirPath,request,null);
      }

      /**
       * Creates or updates a FHIR resource on the specified provider. If the resource exists, it will be updated; otherwise, it will be created.
       * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
       */
      public PhenoMLHttpResponse<FhirResource> upsert(String fhirProviderId, String fhirPath,
          FhirUpsertRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("fhir-provider")
          .addPathSegment(fhirProviderId)
          .addPathSegments("fhir")
          .addPathSegment(fhirPath);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          RequestBody body;
          try {
            body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaType.parse("application/fhir+json"));
          }
          catch(Exception e) {
            throw new RuntimeException(e);
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("PUT", body)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Content-Type", "application/fhir+json")
            .addHeader("Accept", "application/json");
          if (request.getPhenomlOnBehalfOf().isPresent()) {
            _requestBuilder.addHeader("X-Phenoml-On-Behalf-Of", request.getPhenomlOnBehalfOf().get());
          }
          if (request.getPhenomlFhirProvider().isPresent()) {
            _requestBuilder.addHeader("X-Phenoml-Fhir-Provider", request.getPhenomlFhirProvider().get());
          }
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new PhenoMLHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FhirResource.class), response);
            }
            try {
              switch (response.code()) {
                case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PhenoMLApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new PhenoMLException("Network error executing HTTP request", e);
          }
        }

        /**
         * Deletes a FHIR resource from the specified provider.
         * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
         */
        public PhenoMLHttpResponse<Map<String, Object>> delete(String fhirProviderId,
            String fhirPath) {
          return delete(fhirProviderId,fhirPath,FhirDeleteRequest.builder().build());
        }

        /**
         * Deletes a FHIR resource from the specified provider.
         * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
         */
        public PhenoMLHttpResponse<Map<String, Object>> delete(String fhirProviderId,
            String fhirPath, RequestOptions requestOptions) {
          return delete(fhirProviderId,fhirPath,FhirDeleteRequest.builder().build(),requestOptions);
        }

        /**
         * Deletes a FHIR resource from the specified provider.
         * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
         */
        public PhenoMLHttpResponse<Map<String, Object>> delete(String fhirProviderId,
            String fhirPath, FhirDeleteRequest request) {
          return delete(fhirProviderId,fhirPath,request,null);
        }

        /**
         * Deletes a FHIR resource from the specified provider.
         * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
         */
        public PhenoMLHttpResponse<Map<String, Object>> delete(String fhirProviderId,
            String fhirPath, FhirDeleteRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("fhir-provider")
            .addPathSegment(fhirProviderId)
            .addPathSegments("fhir")
            .addPathSegment(fhirPath);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("DELETE", null)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            if (request.getPhenomlOnBehalfOf().isPresent()) {
              _requestBuilder.addHeader("X-Phenoml-On-Behalf-Of", request.getPhenomlOnBehalfOf().get());
            }
            if (request.getPhenomlFhirProvider().isPresent()) {
              _requestBuilder.addHeader("X-Phenoml-Fhir-Provider", request.getPhenomlFhirProvider().get());
            }
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new PhenoMLHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, new TypeReference<Map<String, Object>>() {}), response);
              }
              try {
                switch (response.code()) {
                  case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new PhenoMLApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new PhenoMLException("Network error executing HTTP request", e);
            }
          }

          /**
           * Partially updates a FHIR resource on the specified provider using JSON Patch operations as defined in RFC 6902.
           * <p>The request body should contain an array of JSON Patch operations. Each operation specifies:</p>
           * <ul>
           * <li><code>op</code>: The operation type (add, remove, replace, move, copy, test)</li>
           * <li><code>path</code>: JSON Pointer to the target location in the resource</li>
           * <li><code>value</code>: The value to use (required for add, replace, and test operations)</li>
           * </ul>
           * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
           */
          public PhenoMLHttpResponse<FhirResource> patch(String fhirProviderId, String fhirPath,
              List<FhirPatchRequestBodyItem> body) {
            return patch(fhirProviderId, fhirPath, FhirPatchRequest.builder().body(body).build());
          }

          /**
           * Partially updates a FHIR resource on the specified provider using JSON Patch operations as defined in RFC 6902.
           * <p>The request body should contain an array of JSON Patch operations. Each operation specifies:</p>
           * <ul>
           * <li><code>op</code>: The operation type (add, remove, replace, move, copy, test)</li>
           * <li><code>path</code>: JSON Pointer to the target location in the resource</li>
           * <li><code>value</code>: The value to use (required for add, replace, and test operations)</li>
           * </ul>
           * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
           */
          public PhenoMLHttpResponse<FhirResource> patch(String fhirProviderId, String fhirPath,
              List<FhirPatchRequestBodyItem> body, RequestOptions requestOptions) {
            return patch(fhirProviderId, fhirPath, FhirPatchRequest.builder().body(body).build(), requestOptions);
          }

          /**
           * Partially updates a FHIR resource on the specified provider using JSON Patch operations as defined in RFC 6902.
           * <p>The request body should contain an array of JSON Patch operations. Each operation specifies:</p>
           * <ul>
           * <li><code>op</code>: The operation type (add, remove, replace, move, copy, test)</li>
           * <li><code>path</code>: JSON Pointer to the target location in the resource</li>
           * <li><code>value</code>: The value to use (required for add, replace, and test operations)</li>
           * </ul>
           * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
           */
          public PhenoMLHttpResponse<FhirResource> patch(String fhirProviderId, String fhirPath,
              FhirPatchRequest request) {
            return patch(fhirProviderId,fhirPath,request,null);
          }

          /**
           * Partially updates a FHIR resource on the specified provider using JSON Patch operations as defined in RFC 6902.
           * <p>The request body should contain an array of JSON Patch operations. Each operation specifies:</p>
           * <ul>
           * <li><code>op</code>: The operation type (add, remove, replace, move, copy, test)</li>
           * <li><code>path</code>: JSON Pointer to the target location in the resource</li>
           * <li><code>value</code>: The value to use (required for add, replace, and test operations)</li>
           * </ul>
           * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
           */
          public PhenoMLHttpResponse<FhirResource> patch(String fhirProviderId, String fhirPath,
              FhirPatchRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("fhir-provider")
              .addPathSegment(fhirProviderId)
              .addPathSegments("fhir")
              .addPathSegment(fhirPath);if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              RequestBody body;
              try {
                body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaType.parse("application/json-patch+json"));
              }
              catch(Exception e) {
                throw new RuntimeException(e);
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json-patch+json")
                .addHeader("Accept", "application/json");
              if (request.getPhenomlOnBehalfOf().isPresent()) {
                _requestBuilder.addHeader("X-Phenoml-On-Behalf-Of", request.getPhenomlOnBehalfOf().get());
              }
              if (request.getPhenomlFhirProvider().isPresent()) {
                _requestBuilder.addHeader("X-Phenoml-Fhir-Provider", request.getPhenomlFhirProvider().get());
              }
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new PhenoMLHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FhirResource.class), response);
                }
                try {
                  switch (response.code()) {
                    case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new PhenoMLApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (IOException e) {
                throw new PhenoMLException("Network error executing HTTP request", e);
              }
            }

            /**
             * Executes a FHIR Bundle transaction or batch operation on the specified provider. This allows multiple FHIR resources to be processed in a single request.
             * <p>The request body should contain a valid FHIR Bundle resource with transaction or batch type.</p>
             * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
             */
            public PhenoMLHttpResponse<FhirBundle> executeBundle(String fhirProviderId,
                FhirBundle body) {
              return executeBundle(fhirProviderId, FhirExecuteBundleRequest.builder().body(body).build());
            }

            /**
             * Executes a FHIR Bundle transaction or batch operation on the specified provider. This allows multiple FHIR resources to be processed in a single request.
             * <p>The request body should contain a valid FHIR Bundle resource with transaction or batch type.</p>
             * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
             */
            public PhenoMLHttpResponse<FhirBundle> executeBundle(String fhirProviderId,
                FhirBundle body, RequestOptions requestOptions) {
              return executeBundle(fhirProviderId, FhirExecuteBundleRequest.builder().body(body).build(), requestOptions);
            }

            /**
             * Executes a FHIR Bundle transaction or batch operation on the specified provider. This allows multiple FHIR resources to be processed in a single request.
             * <p>The request body should contain a valid FHIR Bundle resource with transaction or batch type.</p>
             * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
             */
            public PhenoMLHttpResponse<FhirBundle> executeBundle(String fhirProviderId,
                FhirExecuteBundleRequest request) {
              return executeBundle(fhirProviderId,request,null);
            }

            /**
             * Executes a FHIR Bundle transaction or batch operation on the specified provider. This allows multiple FHIR resources to be processed in a single request.
             * <p>The request body should contain a valid FHIR Bundle resource with transaction or batch type.</p>
             * <p>The request is proxied to the configured FHIR server with appropriate authentication headers.</p>
             */
            public PhenoMLHttpResponse<FhirBundle> executeBundle(String fhirProviderId,
                FhirExecuteBundleRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("fhir-provider")
                .addPathSegment(fhirProviderId)
                .addPathSegments("fhir");if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                RequestBody body;
                try {
                  body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaType.parse("application/fhir+json"));
                }
                catch(Exception e) {
                  throw new RuntimeException(e);
                }
                Request.Builder _requestBuilder = new Request.Builder()
                  .url(httpUrl.build())
                  .method("POST", body)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Content-Type", "application/fhir+json")
                  .addHeader("Accept", "application/json");
                if (request.getPhenomlOnBehalfOf().isPresent()) {
                  _requestBuilder.addHeader("X-Phenoml-On-Behalf-Of", request.getPhenomlOnBehalfOf().get());
                }
                if (request.getPhenomlFhirProvider().isPresent()) {
                  _requestBuilder.addHeader("X-Phenoml-Fhir-Provider", request.getPhenomlFhirProvider().get());
                }
                Request okhttpRequest = _requestBuilder.build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    return new PhenoMLHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FhirBundle.class), response);
                  }
                  try {
                    switch (response.code()) {
                      case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new PhenoMLApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (IOException e) {
                  throw new PhenoMLException("Network error executing HTTP request", e);
                }
              }
            }
