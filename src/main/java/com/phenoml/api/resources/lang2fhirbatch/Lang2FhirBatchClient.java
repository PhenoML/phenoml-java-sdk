/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.phenoml.api.resources.lang2fhirbatch;

import com.phenoml.api.core.ClientOptions;
import com.phenoml.api.core.RequestOptions;
import com.phenoml.api.resources.lang2fhirbatch.requests.CreateBatchRequest;
import com.phenoml.api.resources.lang2fhirbatch.requests.GetRequest;
import com.phenoml.api.resources.lang2fhirbatch.requests.GetResultsRequest;
import com.phenoml.api.resources.lang2fhirbatch.requests.ListRequest;
import com.phenoml.api.resources.lang2fhirbatch.requests.UploadItemRequest;
import com.phenoml.api.resources.lang2fhirbatch.types.BatchJob;
import com.phenoml.api.resources.lang2fhirbatch.types.JobDetailResponse;
import com.phenoml.api.resources.lang2fhirbatch.types.JobListResponse;
import com.phenoml.api.resources.lang2fhirbatch.types.ResultsPageResponse;
import com.phenoml.api.resources.lang2fhirbatch.types.UploadItemResponse;
import java.io.File;
import java.io.InputStream;
import java.util.Map;
import java.util.Optional;
import okhttp3.MediaType;

public class Lang2FhirBatchClient {
    protected final ClientOptions clientOptions;

    private final RawLang2FhirBatchClient rawClient;

    public Lang2FhirBatchClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawLang2FhirBatchClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawLang2FhirBatchClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Returns a page of the instance's batch jobs, newest first, without
     * per-job counts. Jobs are shared across the instance's credentials, so
     * this lists every batch job on the instance, not just the calling
     * credential's.
     */
    public JobListResponse list() {
        return this.rawClient.list().body();
    }

    /**
     * Returns a page of the instance's batch jobs, newest first, without
     * per-job counts. Jobs are shared across the instance's credentials, so
     * this lists every batch job on the instance, not just the calling
     * credential's.
     */
    public JobListResponse list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).body();
    }

    /**
     * Returns a page of the instance's batch jobs, newest first, without
     * per-job counts. Jobs are shared across the instance's credentials, so
     * this lists every batch job on the instance, not just the calling
     * credential's.
     */
    public JobListResponse list(ListRequest request) {
        return this.rawClient.list(request).body();
    }

    /**
     * Returns a page of the instance's batch jobs, newest first, without
     * per-job counts. Jobs are shared across the instance's credentials, so
     * this lists every batch job on the instance, not just the calling
     * credential's.
     */
    public JobListResponse list(ListRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).body();
    }

    /**
     * Opens an empty batch job. Items arrive on later upload calls and the set
     * is sealed at finalize.
     * <p>Supplying <code>request_id</code> makes the create idempotent on that token: a
     * retried submit whose response was lost returns the original job rather
     * than opening a second one. This dedupe is scoped to the calling
     * credential.</p>
     * <p>An instance may hold at most 4 active (pending or processing) jobs at
     * once; a create past that limit returns <code>409</code>. The limit is instance-wide
     * — jobs are shared across the instance's credentials — so another
     * credential's jobs count against it.</p>
     */
    public BatchJob create() {
        return this.rawClient.create().body();
    }

    /**
     * Opens an empty batch job. Items arrive on later upload calls and the set
     * is sealed at finalize.
     * <p>Supplying <code>request_id</code> makes the create idempotent on that token: a
     * retried submit whose response was lost returns the original job rather
     * than opening a second one. This dedupe is scoped to the calling
     * credential.</p>
     * <p>An instance may hold at most 4 active (pending or processing) jobs at
     * once; a create past that limit returns <code>409</code>. The limit is instance-wide
     * — jobs are shared across the instance's credentials — so another
     * credential's jobs count against it.</p>
     */
    public BatchJob create(RequestOptions requestOptions) {
        return this.rawClient.create(requestOptions).body();
    }

    /**
     * Opens an empty batch job. Items arrive on later upload calls and the set
     * is sealed at finalize.
     * <p>Supplying <code>request_id</code> makes the create idempotent on that token: a
     * retried submit whose response was lost returns the original job rather
     * than opening a second one. This dedupe is scoped to the calling
     * credential.</p>
     * <p>An instance may hold at most 4 active (pending or processing) jobs at
     * once; a create past that limit returns <code>409</code>. The limit is instance-wide
     * — jobs are shared across the instance's credentials — so another
     * credential's jobs count against it.</p>
     */
    public BatchJob create(CreateBatchRequest request) {
        return this.rawClient.create(request).body();
    }

    /**
     * Opens an empty batch job. Items arrive on later upload calls and the set
     * is sealed at finalize.
     * <p>Supplying <code>request_id</code> makes the create idempotent on that token: a
     * retried submit whose response was lost returns the original job rather
     * than opening a second one. This dedupe is scoped to the calling
     * credential.</p>
     * <p>An instance may hold at most 4 active (pending or processing) jobs at
     * once; a create past that limit returns <code>409</code>. The limit is instance-wide
     * — jobs are shared across the instance's credentials — so another
     * credential's jobs count against it.</p>
     */
    public BatchJob create(CreateBatchRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(request, requestOptions).body();
    }

    /**
     * Stores one item of a job from a multipart upload. A batch's items arrive
     * one per request. The item carries <strong>either</strong> a <code>document</code> extraction
     * (whose input file rides as raw bytes in the <code>file</code> part) <strong>or</strong> a
     * <code>create</code> extraction (JSON only, no file).
     * <p>The upload enforces these rules:</p>
     * <ul>
     * <li>Set <strong>exactly one</strong> of <code>document</code> or <code>create</code>. Setting both, or
     * neither, is a <code>400</code>.</li>
     * <li>When <code>document</code> is set, <code>file</code> is <strong>required</strong> — it supplies the
     * document's binary content (PDF or image).</li>
     * <li>When <code>create</code> is set, <code>file</code> is <strong>forbidden</strong> — a create item carries
     * no file.</li>
     * <li><code>document</code> and <code>create</code> must each be a JSON <strong>object</strong>.</li>
     * </ul>
     * <p>Only the item's structure is checked here: the fields inside <code>document</code>
     * or <code>create</code> are not validated at upload. A body that is well-formed JSON
     * but not a valid request for its endpoint is still accepted with <code>202</code>
     * and fails later during processing, recorded as an item <code>error</code>. A
     * wrong-typed field the endpoint cannot decode fails as <code>invalid_input</code>; a
     * body that decodes but the pipeline rejects (for example, a missing
     * required field) fails as <code>processing_failed</code>.</p>
     * <p>Supplying <code>request_id</code> makes the upload idempotent on that token. A
     * re-upload under the same token overwrites the same item rather than
     * adding a second, so a client that lost an upload's response can safely
     * re-send it. The response's <code>deduplicated</code> is <code>true</code> only when the
     * re-uploaded payload matches the one already stored; a same-token upload
     * with a changed payload overwrites in place and returns <code>false</code>.</p>
     * <p>Set a <code>request_id</code> on <strong>every</strong> upload: re-sending under the same token
     * is the only way to repair a lost or incomplete upload, including the one
     * a finalize <code>409</code> reports. Without one, a re-send adds a new item instead
     * of replacing the missing one, and the job cannot be finalized.</p>
     * <p>Uploads are rejected once the job has been finalized (<code>409</code>), once it
     * holds its 500-item limit (<code>409</code>), or when the item is too large (<code>413</code> —
     * see the raw-file limit in the API description).</p>
     */
    public UploadItemResponse uploadItem(String jobId, Optional<File> file) {
        return this.rawClient.uploadItem(jobId, file).body();
    }

    /**
     * Stores one item of a job from a multipart upload. A batch's items arrive
     * one per request. The item carries <strong>either</strong> a <code>document</code> extraction
     * (whose input file rides as raw bytes in the <code>file</code> part) <strong>or</strong> a
     * <code>create</code> extraction (JSON only, no file).
     * <p>The upload enforces these rules:</p>
     * <ul>
     * <li>Set <strong>exactly one</strong> of <code>document</code> or <code>create</code>. Setting both, or
     * neither, is a <code>400</code>.</li>
     * <li>When <code>document</code> is set, <code>file</code> is <strong>required</strong> — it supplies the
     * document's binary content (PDF or image).</li>
     * <li>When <code>create</code> is set, <code>file</code> is <strong>forbidden</strong> — a create item carries
     * no file.</li>
     * <li><code>document</code> and <code>create</code> must each be a JSON <strong>object</strong>.</li>
     * </ul>
     * <p>Only the item's structure is checked here: the fields inside <code>document</code>
     * or <code>create</code> are not validated at upload. A body that is well-formed JSON
     * but not a valid request for its endpoint is still accepted with <code>202</code>
     * and fails later during processing, recorded as an item <code>error</code>. A
     * wrong-typed field the endpoint cannot decode fails as <code>invalid_input</code>; a
     * body that decodes but the pipeline rejects (for example, a missing
     * required field) fails as <code>processing_failed</code>.</p>
     * <p>Supplying <code>request_id</code> makes the upload idempotent on that token. A
     * re-upload under the same token overwrites the same item rather than
     * adding a second, so a client that lost an upload's response can safely
     * re-send it. The response's <code>deduplicated</code> is <code>true</code> only when the
     * re-uploaded payload matches the one already stored; a same-token upload
     * with a changed payload overwrites in place and returns <code>false</code>.</p>
     * <p>Set a <code>request_id</code> on <strong>every</strong> upload: re-sending under the same token
     * is the only way to repair a lost or incomplete upload, including the one
     * a finalize <code>409</code> reports. Without one, a re-send adds a new item instead
     * of replacing the missing one, and the job cannot be finalized.</p>
     * <p>Uploads are rejected once the job has been finalized (<code>409</code>), once it
     * holds its 500-item limit (<code>409</code>), or when the item is too large (<code>413</code> —
     * see the raw-file limit in the API description).</p>
     */
    public UploadItemResponse uploadItem(String jobId, Optional<File> file, RequestOptions requestOptions) {
        return this.rawClient.uploadItem(jobId, file, requestOptions).body();
    }

    /**
     * Stores one item of a job from a multipart upload. A batch's items arrive
     * one per request. The item carries <strong>either</strong> a <code>document</code> extraction
     * (whose input file rides as raw bytes in the <code>file</code> part) <strong>or</strong> a
     * <code>create</code> extraction (JSON only, no file).
     * <p>The upload enforces these rules:</p>
     * <ul>
     * <li>Set <strong>exactly one</strong> of <code>document</code> or <code>create</code>. Setting both, or
     * neither, is a <code>400</code>.</li>
     * <li>When <code>document</code> is set, <code>file</code> is <strong>required</strong> — it supplies the
     * document's binary content (PDF or image).</li>
     * <li>When <code>create</code> is set, <code>file</code> is <strong>forbidden</strong> — a create item carries
     * no file.</li>
     * <li><code>document</code> and <code>create</code> must each be a JSON <strong>object</strong>.</li>
     * </ul>
     * <p>Only the item's structure is checked here: the fields inside <code>document</code>
     * or <code>create</code> are not validated at upload. A body that is well-formed JSON
     * but not a valid request for its endpoint is still accepted with <code>202</code>
     * and fails later during processing, recorded as an item <code>error</code>. A
     * wrong-typed field the endpoint cannot decode fails as <code>invalid_input</code>; a
     * body that decodes but the pipeline rejects (for example, a missing
     * required field) fails as <code>processing_failed</code>.</p>
     * <p>Supplying <code>request_id</code> makes the upload idempotent on that token. A
     * re-upload under the same token overwrites the same item rather than
     * adding a second, so a client that lost an upload's response can safely
     * re-send it. The response's <code>deduplicated</code> is <code>true</code> only when the
     * re-uploaded payload matches the one already stored; a same-token upload
     * with a changed payload overwrites in place and returns <code>false</code>.</p>
     * <p>Set a <code>request_id</code> on <strong>every</strong> upload: re-sending under the same token
     * is the only way to repair a lost or incomplete upload, including the one
     * a finalize <code>409</code> reports. Without one, a re-send adds a new item instead
     * of replacing the missing one, and the job cannot be finalized.</p>
     * <p>Uploads are rejected once the job has been finalized (<code>409</code>), once it
     * holds its 500-item limit (<code>409</code>), or when the item is too large (<code>413</code> —
     * see the raw-file limit in the API description).</p>
     */
    public UploadItemResponse uploadItem(String jobId, Optional<File> file, UploadItemRequest request) {
        return this.rawClient.uploadItem(jobId, file, request).body();
    }

    /**
     * Stores one item of a job from a multipart upload. A batch's items arrive
     * one per request. The item carries <strong>either</strong> a <code>document</code> extraction
     * (whose input file rides as raw bytes in the <code>file</code> part) <strong>or</strong> a
     * <code>create</code> extraction (JSON only, no file).
     * <p>The upload enforces these rules:</p>
     * <ul>
     * <li>Set <strong>exactly one</strong> of <code>document</code> or <code>create</code>. Setting both, or
     * neither, is a <code>400</code>.</li>
     * <li>When <code>document</code> is set, <code>file</code> is <strong>required</strong> — it supplies the
     * document's binary content (PDF or image).</li>
     * <li>When <code>create</code> is set, <code>file</code> is <strong>forbidden</strong> — a create item carries
     * no file.</li>
     * <li><code>document</code> and <code>create</code> must each be a JSON <strong>object</strong>.</li>
     * </ul>
     * <p>Only the item's structure is checked here: the fields inside <code>document</code>
     * or <code>create</code> are not validated at upload. A body that is well-formed JSON
     * but not a valid request for its endpoint is still accepted with <code>202</code>
     * and fails later during processing, recorded as an item <code>error</code>. A
     * wrong-typed field the endpoint cannot decode fails as <code>invalid_input</code>; a
     * body that decodes but the pipeline rejects (for example, a missing
     * required field) fails as <code>processing_failed</code>.</p>
     * <p>Supplying <code>request_id</code> makes the upload idempotent on that token. A
     * re-upload under the same token overwrites the same item rather than
     * adding a second, so a client that lost an upload's response can safely
     * re-send it. The response's <code>deduplicated</code> is <code>true</code> only when the
     * re-uploaded payload matches the one already stored; a same-token upload
     * with a changed payload overwrites in place and returns <code>false</code>.</p>
     * <p>Set a <code>request_id</code> on <strong>every</strong> upload: re-sending under the same token
     * is the only way to repair a lost or incomplete upload, including the one
     * a finalize <code>409</code> reports. Without one, a re-send adds a new item instead
     * of replacing the missing one, and the job cannot be finalized.</p>
     * <p>Uploads are rejected once the job has been finalized (<code>409</code>), once it
     * holds its 500-item limit (<code>409</code>), or when the item is too large (<code>413</code> —
     * see the raw-file limit in the API description).</p>
     */
    public UploadItemResponse uploadItem(
            String jobId, Optional<File> file, UploadItemRequest request, RequestOptions requestOptions) {
        return this.rawClient.uploadItem(jobId, file, request, requestOptions).body();
    }

    public UploadItemResponse uploadItem(String jobId, Optional<File> file, InputStream stream, String filename) {
        return this.rawClient.uploadItem(jobId, file, stream, filename).body();
    }

    public UploadItemResponse uploadItem(
            String jobId, Optional<File> file, InputStream stream, String filename, MediaType mediaType) {
        return this.rawClient
                .uploadItem(jobId, file, stream, filename, mediaType)
                .body();
    }

    public UploadItemResponse uploadItem(
            String jobId, Optional<File> file, InputStream stream, String filename, RequestOptions requestOptions) {
        return this.rawClient
                .uploadItem(jobId, file, stream, filename, requestOptions)
                .body();
    }

    public UploadItemResponse uploadItem(
            String jobId,
            Optional<File> file,
            InputStream stream,
            String filename,
            MediaType mediaType,
            RequestOptions requestOptions) {
        return this.rawClient
                .uploadItem(jobId, file, stream, filename, mediaType, requestOptions)
                .body();
    }

    /**
     * Seals the job's item set and starts processing. Takes no request body.
     * Finalize is idempotent: a retried finalize succeeds again.
     * <p>If a previous upload did not complete, finalize returns a <code>409</code>; re-send
     * the missing upload (with the same <code>request_id</code>), then finalize.
     * Finalizing a job with no items is a <code>400</code>.</p>
     */
    public BatchJob finalize(String jobId) {
        return this.rawClient.finalize(jobId).body();
    }

    /**
     * Seals the job's item set and starts processing. Takes no request body.
     * Finalize is idempotent: a retried finalize succeeds again.
     * <p>If a previous upload did not complete, finalize returns a <code>409</code>; re-send
     * the missing upload (with the same <code>request_id</code>), then finalize.
     * Finalizing a job with no items is a <code>400</code>.</p>
     */
    public BatchJob finalize(String jobId, RequestOptions requestOptions) {
        return this.rawClient.finalize(jobId, requestOptions).body();
    }

    /**
     * Returns a job's record, its per-status item counts, and one page of
     * per-item statuses.
     * <p>Items are listed in a stable order that is not upload order and is the
     * same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public JobDetailResponse get(String jobId) {
        return this.rawClient.get(jobId).body();
    }

    /**
     * Returns a job's record, its per-status item counts, and one page of
     * per-item statuses.
     * <p>Items are listed in a stable order that is not upload order and is the
     * same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public JobDetailResponse get(String jobId, RequestOptions requestOptions) {
        return this.rawClient.get(jobId, requestOptions).body();
    }

    /**
     * Returns a job's record, its per-status item counts, and one page of
     * per-item statuses.
     * <p>Items are listed in a stable order that is not upload order and is the
     * same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public JobDetailResponse get(String jobId, GetRequest request) {
        return this.rawClient.get(jobId, request).body();
    }

    /**
     * Returns a job's record, its per-status item counts, and one page of
     * per-item statuses.
     * <p>Items are listed in a stable order that is not upload order and is the
     * same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public JobDetailResponse get(String jobId, GetRequest request, RequestOptions requestOptions) {
        return this.rawClient.get(jobId, request, requestOptions).body();
    }

    /**
     * A lighter status page. Returns the same per-item status entries as
     * <code>GET /lang2fhir/batch/{job_id}</code>, but without the job record or counts,
     * and the entries carry <code>result_size</code> rather than any result content. Use
     * each entry's <code>item_id</code> to fetch that item's result from
     * <code>GET /lang2fhir/batch/{job_id}/results/{item_id}</code>.
     * <p>Entries are listed in a stable order that is not upload order and is
     * the same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public ResultsPageResponse getResults(String jobId) {
        return this.rawClient.getResults(jobId).body();
    }

    /**
     * A lighter status page. Returns the same per-item status entries as
     * <code>GET /lang2fhir/batch/{job_id}</code>, but without the job record or counts,
     * and the entries carry <code>result_size</code> rather than any result content. Use
     * each entry's <code>item_id</code> to fetch that item's result from
     * <code>GET /lang2fhir/batch/{job_id}/results/{item_id}</code>.
     * <p>Entries are listed in a stable order that is not upload order and is
     * the same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public ResultsPageResponse getResults(String jobId, RequestOptions requestOptions) {
        return this.rawClient.getResults(jobId, requestOptions).body();
    }

    /**
     * A lighter status page. Returns the same per-item status entries as
     * <code>GET /lang2fhir/batch/{job_id}</code>, but without the job record or counts,
     * and the entries carry <code>result_size</code> rather than any result content. Use
     * each entry's <code>item_id</code> to fetch that item's result from
     * <code>GET /lang2fhir/batch/{job_id}/results/{item_id}</code>.
     * <p>Entries are listed in a stable order that is not upload order and is
     * the same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public ResultsPageResponse getResults(String jobId, GetResultsRequest request) {
        return this.rawClient.getResults(jobId, request).body();
    }

    /**
     * A lighter status page. Returns the same per-item status entries as
     * <code>GET /lang2fhir/batch/{job_id}</code>, but without the job record or counts,
     * and the entries carry <code>result_size</code> rather than any result content. Use
     * each entry's <code>item_id</code> to fetch that item's result from
     * <code>GET /lang2fhir/batch/{job_id}/results/{item_id}</code>.
     * <p>Entries are listed in a stable order that is not upload order and is
     * the same across pages. Match each entry to your own records by its <code>id</code>
     * (your correlation label) or <code>item_id</code> (from the upload response),
     * never by position.</p>
     */
    public ResultsPageResponse getResults(String jobId, GetResultsRequest request, RequestOptions requestOptions) {
        return this.rawClient.getResults(jobId, request, requestOptions).body();
    }

    /**
     * Streams one item's stored result bytes verbatim as <code>application/json</code>.
     * The body is the response the item's synchronous multi endpoint would have
     * returned — a <code>DocumentMultiResponse</code> for a document item or a
     * <code>CreateMultiResponse</code> for a create item.
     * <p>Only a succeeded item has a result: an item that has not succeeded
     * (pending, processing, or failed) is a <code>409</code>, and a result that has
     * expired is a <code>404</code>.</p>
     */
    public Map<String, Object> getResult(String jobId, String itemId) {
        return this.rawClient.getResult(jobId, itemId).body();
    }

    /**
     * Streams one item's stored result bytes verbatim as <code>application/json</code>.
     * The body is the response the item's synchronous multi endpoint would have
     * returned — a <code>DocumentMultiResponse</code> for a document item or a
     * <code>CreateMultiResponse</code> for a create item.
     * <p>Only a succeeded item has a result: an item that has not succeeded
     * (pending, processing, or failed) is a <code>409</code>, and a result that has
     * expired is a <code>404</code>.</p>
     */
    public Map<String, Object> getResult(String jobId, String itemId, RequestOptions requestOptions) {
        return this.rawClient.getResult(jobId, itemId, requestOptions).body();
    }
}
