/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.phenoml.api.resources.lang2fhirbatch.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.phenoml.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = UploadItemRequest.Builder.class)
public final class UploadItemRequest {
    private final Optional<Map<String, Object>> document;

    private final Optional<Map<String, Object>> create;

    private final Optional<String> requestId;

    private final Optional<String> id;

    private final Map<String, Object> additionalProperties;

    private UploadItemRequest(
            Optional<Map<String, Object>> document,
            Optional<Map<String, Object>> create,
            Optional<String> requestId,
            Optional<String> id,
            Map<String, Object> additionalProperties) {
        this.document = document;
        this.create = create;
        this.requestId = requestId;
        this.id = id;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The JSON body of <code>POST /lang2fhir/document/multi</code>, <strong>without</strong>
     * its base64 <code>content</code> field — the uploaded <code>file</code> supplies the
     * content. Accepts that endpoint's fields (<code>version</code>, <code>provider</code>,
     * <code>patient_reference</code>, <code>implementation_guide</code>, <code>detection_effort</code>,
     * <code>validation_method</code>, <code>config</code>). This is the <strong>multi</strong>-resource
     * body: it has no single-<code>resource</code> field, and the item's result
     * is a <code>DocumentMultiResponse</code> (a Bundle of resources). Mutually
     * exclusive with <code>create</code>; requires <code>file</code>.
     */
    @JsonProperty("document")
    public Optional<Map<String, Object>> getDocument() {
        return document;
    }

    /**
     * @return The JSON body of <code>POST /lang2fhir/create/multi</code>. Accepts that
     * endpoint's fields (<code>text</code>, <code>version</code>, <code>provider</code>,
     * <code>patient_reference</code>, <code>implementation_guide</code>, <code>detection_effort</code>,
     * <code>validation_method</code>, <code>resource_review</code>). This is the
     * <strong>multi</strong>-resource body: it has no single-<code>resource</code> field, and
     * the item's result is a <code>CreateMultiResponse</code> (a Bundle of
     * resources). Mutually exclusive with <code>document</code>; must <strong>not</strong> be
     * accompanied by a <code>file</code>.
     */
    @JsonProperty("create")
    public Optional<Map<String, Object>> getCreate() {
        return create;
    }

    /**
     * @return Optional idempotency token (max 256 bytes). Re-uploading under
     * the same token overwrites the same item instead of adding a
     * new one. The token is scoped to this job; the same token in
     * another job is independent and creates a separate item.
     */
    @JsonProperty("request_id")
    public Optional<String> getRequestId() {
        return requestId;
    }

    /**
     * @return Optional caller-supplied correlation label (max 512 bytes),
     * echoed back on status and result listings so you can match the
     * server's item_id to your own record.
     */
    @JsonProperty("id")
    public Optional<String> getId() {
        return id;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof UploadItemRequest && equalTo((UploadItemRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(UploadItemRequest other) {
        return document.equals(other.document)
                && create.equals(other.create)
                && requestId.equals(other.requestId)
                && id.equals(other.id);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.document, this.create, this.requestId, this.id);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static Builder builder() {
        return new Builder();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder {
        private Optional<Map<String, Object>> document = Optional.empty();

        private Optional<Map<String, Object>> create = Optional.empty();

        private Optional<String> requestId = Optional.empty();

        private Optional<String> id = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        public Builder from(UploadItemRequest other) {
            document(other.getDocument());
            create(other.getCreate());
            requestId(other.getRequestId());
            id(other.getId());
            return this;
        }

        /**
         * <p>The JSON body of <code>POST /lang2fhir/document/multi</code>, <strong>without</strong>
         * its base64 <code>content</code> field — the uploaded <code>file</code> supplies the
         * content. Accepts that endpoint's fields (<code>version</code>, <code>provider</code>,
         * <code>patient_reference</code>, <code>implementation_guide</code>, <code>detection_effort</code>,
         * <code>validation_method</code>, <code>config</code>). This is the <strong>multi</strong>-resource
         * body: it has no single-<code>resource</code> field, and the item's result
         * is a <code>DocumentMultiResponse</code> (a Bundle of resources). Mutually
         * exclusive with <code>create</code>; requires <code>file</code>.</p>
         */
        @JsonSetter(value = "document", nulls = Nulls.SKIP)
        public Builder document(Optional<Map<String, Object>> document) {
            this.document = document;
            return this;
        }

        public Builder document(Map<String, Object> document) {
            this.document = Optional.ofNullable(document);
            return this;
        }

        /**
         * <p>The JSON body of <code>POST /lang2fhir/create/multi</code>. Accepts that
         * endpoint's fields (<code>text</code>, <code>version</code>, <code>provider</code>,
         * <code>patient_reference</code>, <code>implementation_guide</code>, <code>detection_effort</code>,
         * <code>validation_method</code>, <code>resource_review</code>). This is the
         * <strong>multi</strong>-resource body: it has no single-<code>resource</code> field, and
         * the item's result is a <code>CreateMultiResponse</code> (a Bundle of
         * resources). Mutually exclusive with <code>document</code>; must <strong>not</strong> be
         * accompanied by a <code>file</code>.</p>
         */
        @JsonSetter(value = "create", nulls = Nulls.SKIP)
        public Builder create(Optional<Map<String, Object>> create) {
            this.create = create;
            return this;
        }

        public Builder create(Map<String, Object> create) {
            this.create = Optional.ofNullable(create);
            return this;
        }

        /**
         * <p>Optional idempotency token (max 256 bytes). Re-uploading under
         * the same token overwrites the same item instead of adding a
         * new one. The token is scoped to this job; the same token in
         * another job is independent and creates a separate item.</p>
         */
        @JsonSetter(value = "request_id", nulls = Nulls.SKIP)
        public Builder requestId(Optional<String> requestId) {
            this.requestId = requestId;
            return this;
        }

        public Builder requestId(String requestId) {
            this.requestId = Optional.ofNullable(requestId);
            return this;
        }

        /**
         * <p>Optional caller-supplied correlation label (max 512 bytes),
         * echoed back on status and result listings so you can match the
         * server's item_id to your own record.</p>
         */
        @JsonSetter(value = "id", nulls = Nulls.SKIP)
        public Builder id(Optional<String> id) {
            this.id = id;
            return this;
        }

        public Builder id(String id) {
            this.id = Optional.ofNullable(id);
            return this;
        }

        public UploadItemRequest build() {
            return new UploadItemRequest(document, create, requestId, id, additionalProperties);
        }

        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
